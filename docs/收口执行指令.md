现在执行“阶段需求 01：现状收敛与口径统一”的最终收口工作。

这次任务必须一次性完成，不能再停留在分析层，不能再输出“待人工拍板”。人工拍板已完成，你必须直接按以下已定结论落到仓库文件中。

====================
一、已定人工结论（你必须直接执行，不得再讨论）
====================

1. `/project:*` 采用严格字面禁用标准
   - 结论：仓内任何文件中都不允许再出现 `/project:*` 字面
   - 这不是“不能作为入口”而已，而是“连历史说明、迁移说明、禁用说明中也不能出现该字面”
   - 你的任务是把仓内所有 `/project:*` 相关字面全部清理干净

2. `example-login` 采用占位演示示例定位
   - 结论：第一阶段允许它不是“真实可运行闭环示例”
   - 它当前正式定位为：占位演示示例
   - 第一阶段验收允许占位示例存在
   - 不要求现在升级为真实业务闭环
   - 真实示例留到后续阶段处理，不在本阶段完成

3. hooks 注册口径必须收口为“双层事实”
   - 项目治理事实源：项目内文档只能写“hooks 实际执行依赖用户级 ~/.claude/settings.json 配置”
   - 运行时接入条件：用户级 ~/.claude/settings.json 是真实加载位置
   - 不得再把项目级 `.claude/settings.json` 写成 hooks 注册位置
   - 必须明确区分：
     - 项目内治理文档写的是“环境接入条件”
     - 用户级配置才是“运行时生效位置”

4. feature.json 批准字段命名必须统一
   - 只允许使用：
     - approved_by
     - approved_at
     - approved_method
     - approved_note
   - 不允许再出现：
     - approval_method
     - approval_note
     - 其他变体命名

====================
二、你本次任务的唯一目标
====================

只做“现状收敛与口径统一”。

也就是：
把当前仓库中“需求文档、验收清单、审查报告、CLAUDE.md、rules、模板、示例文件”里的分叉点全部收口成单一口径。

本次不追求新增能力，只追求：
- 后续阶段不再建立在歧义上
- 第一阶段验收口径明确
- 所有关键文档对同一事实的表述一致
- 后续阶段可以直接基于本次结果继续推进

====================
三、只允许读取的范围
====================

只读取以下范围，不要主动扩展：

- docs/需求.md
- docs/spec-system/验收清单.md
- docs/spec-system/验收清单审查报告.md
- CLAUDE.md
- .claude/rules/*.md
- .claude/hooks/*.js
- docs/spec-system/模板/AI执行版/feature.json
- features/example-login/*

除上述范围外，不要主动读取更多文件，不要自行扩大审计面。

====================
四、只允许修改的范围
====================

只允许修改：

- docs/spec-system/**
- docs/需求.md
- CLAUDE.md
- .claude/rules/*.md
- docs/spec-system/模板/AI执行版/feature.json
- features/example-login/*.md
- features/example-login/feature.json

不得修改其他范围。

====================
五、禁止事项
====================

严格禁止：

1. 修改任何业务代码
2. 修改 hooks 逻辑实现
3. 新增 CI / Git / deploy / release 能力
4. 扩展 agents / subagents 体系
5. 新增第二阶段能力
6. 再次讨论“该不该这么做”
7. 输出新的长篇总报告
8. 只停留在会话口头说明而不落仓库文件

====================
六、你必须解决的 5 个口径问题
====================

你必须在本次执行中，把以下 5 个问题全部收口到仓库文件中：

1. `/project:*` 的正式验收口径
   - 已定：仓内任何字面都不能出现
   - 你要做的是：删掉仓内所有 `/project:*` 字面，并同步更新相关验收表述

2. hooks 注册的正式事实源
   - 已定：运行时依赖用户级 ~/.claude/settings.json
   - 你要做的是：在项目文档中明确写成“环境接入条件”，不要误写为项目级注册

3. 批准字段命名统一
   - 已定：只允许 approved_by / approved_at / approved_method / approved_note
   - 你要做的是：模板、文档、示例全部统一命名，不允许混用 approval_*

4. `example-login` 的正式定位
   - 已定：占位演示示例
   - 你要做的是：把验收清单、审查报告、示例自身描述统一改成这个定位

5. 第一阶段是否允许占位示例
   - 已定：允许
   - 你要做的是：在验收清单与相关文档中明确写出这一点，避免后续再争论

====================
七、你必须完成的实际动作
====================

请按以下顺序执行，不要跳步。

--------------------------------
第 1 步：先做最小审阅确认
--------------------------------

只基于允许读取的范围，快速确认当前以下几类分叉还在哪些文件里存在：

- `/project:*` 字面残留
- hooks 注册位置表述不一致
- approval_* / approved_* 命名混用
- example-login 被写成真实示例还是占位示例
- 第一阶段验收是否允许占位示例的表述不一致

注意：
- 这一步只做最小定位
- 不要再输出长篇审计报告
- 只要给出“准备修改哪些文件、每个文件要解决什么分叉”即可

--------------------------------
第 2 步：直接修改文件
--------------------------------

你必须直接修改文件，把口径落到仓库中。

修改目标至少包括：

A. `docs/spec-system/验收清单.md`
- 删除与 `/project:*` 宽松口径相关的表述
- 明确：仓内任何 `/project:*` 字面都不允许存在
- 明确：第一阶段允许占位演示示例
- 明确：example-login 当前按占位演示示例验收
- 明确：hooks 运行时依赖用户级 ~/.claude/settings.json，项目内文档仅记录该环境接入条件
- 明确：批准字段唯一命名

B. `docs/spec-system/验收清单审查报告.md`
- 删除“待人工拍板”相关表述
- 改成已定结论
- 把 `/project:*` 的建议方案改为已定标准：字面清零
- 把 example-login 的建议方案改为已定标准：占位演示示例
- 把最终建议改成不再依赖这些待拍板项

C. `docs/需求.md`
- 如存在与上述口径冲突的表述，必须同步修正
- 让需求文档对第一阶段的口径与验收文档保持一致

D. `CLAUDE.md`
- 清除任何 `/project:*` 字面
- 若需要表达“旧命令已废弃”，也不得再直接写 `/project:*`
- 用不含旧字面的方式改写

E. `.claude/rules/*.md`
- 清理所有 `/project:*` 字面
- 统一 hooks 注册的口径描述
- 统一批准字段命名
- 如果 rules 中对 example-login 或占位示例有表述不一致，也要同步修正

F. `docs/spec-system/模板/AI执行版/feature.json`
- 确认只使用 approved_by / approved_at / approved_method / approved_note
- 不允许混入 approval_* 命名
- 如模板文字说明存在分叉，也一起修正

G. `features/example-login/*.md` 与 `features/example-login/feature.json`
- 明确写清它是“占位演示示例”
- 不要再留下会让人误解为“真实闭环示例”的表述
- 使其与验收清单、审查报告、需求文档完全一致

--------------------------------
第 3 步：清除 `/project:*` 字面
--------------------------------

这是本次强制动作，必须完成。

要求：
- 仓内所有位于允许修改范围内的 `/project:*` 字面必须全部消失
- 不能保留在禁用说明、迁移说明、历史对照、审计报告里
- 如果必须表达“旧体系已废弃”，请改写成不直接出现旧字面的描述

--------------------------------
第 4 步：统一 hooks 注册口径
--------------------------------

必须统一成下面这套说法，且所有相关文档都一致：

- hooks 的真实运行时加载位置：用户级 `~/.claude/settings.json`
- 项目仓库中的文档只能把这件事描述为：环境接入条件 / 运行前提
- 项目级 `.claude/settings.json` 不是 hooks 注册位置
- 不得再混淆“项目治理事实源”和“运行时环境配置”

--------------------------------
第 5 步：统一 example-login 口径
--------------------------------

必须统一成下面这套说法：

- example-login 是第一阶段的占位演示示例
- 当前不作为真实业务闭环示例验收
- 第一阶段允许占位演示示例
- 真实可运行示例留到后续阶段再做

--------------------------------
第 6 步：统一批准字段命名
--------------------------------

必须统一成：
- approved_by
- approved_at
- approved_method
- approved_note

要求：
- 所有允许修改范围内的文档、模板、示例文件，都不得再出现 approval_by / approval_at / approval_method / approval_note 这类变体
- 若只是中文说明提到了旧命名，也要改掉

--------------------------------
第 7 步：完成后做最小复核
--------------------------------

完成所有修改后，你必须做最小复核，并明确输出：

1. 你修改了哪些文件
2. 每个文件收口了什么分叉
3. `/project:*` 字面是否已在允许修改范围内清零
4. hooks 注册口径是否已统一
5. 批准字段命名是否已统一
6. example-login 定位是否已统一为占位演示示例
7. 第一阶段是否已明确允许占位示例

注意：
- 不要再生成新的长报告
- 只做“收口完成说明”

====================
八、输出要求
====================

1. 全程中文
2. 不要问我“是否继续”
3. 不要输出分析大段空话
4. 先改文件，再给结果
5. 最后只输出：
   - 修改文件清单
   - 每个文件修改目的
   - 统一后的 5 条正式结论
   - 是否已达到“阶段 01 完成”状态

====================
九、完成标准
====================

满足以下条件才算你完成本次任务：

1. 5 个口径问题都有唯一结论
2. 这些结论已经落到仓库文件里
3. 允许修改范围内不再残留 `/project:*` 字面
4. 不再存在 approved_* / approval_* 混用
5. example-login 的定位不再有歧义
6. 后续阶段文档可以直接基于本次结果推进，不需要再次猜测

现在直接开始执行。