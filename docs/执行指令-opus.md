你现在要从零开始执行本项目的“治理体系第一阶段最小落地”任务。

注意：你不能依赖任何旧会话、旧上下文、旧缓存、旧口头约定。你只能以当前工作区文件内容和我本条指令为准，自行重新审计、重新判断、重新输出。

====================
一、已知事实（视为当前任务前提）
====================

1. 当前任务目标不是业务开发，而是“治理体系第一阶段最小落地”。
2. 当前阶段未经我确认，不允许修改任何业务代码。
3. 第一阶段只允许创建或整理以下内容：
   - 最小 CLAUDE.md
   - 核心 rules
   - 2 个 gate hooks
   - 小白版 4 份模板
   - AI 执行版 4 份模板
   - feature.json 模板
   - 1 个最小示例
   - 迁移说明
   - 基本验收清单
   - command / skill / agent 的保留与迁移方案
4. 第一阶段禁止直接创建以下内容：
   - 完整自动修复循环
   - 完整 agents 集合
   - deploy 类能力
   - 大量兼容 commands
   - 高风险自动执行脚本
5. 第二阶段必须在第一阶段验收后再做，Claude 默认不得自动进入第二阶段。
6. hooks 现状已经有明确结论：
   - Claude Code v2.1.85 不从项目级 .claude/settings.json 加载 hooks
   - 仅从用户级 ~/.claude/settings.json 加载 hooks
   - gate-write.js 和 gate-bash.js 已验证生效
   - tests/verify-hooks.ps1 可作为回归验证工具
   - .claude/hooks/test-hook.js 属于调试残留，可删除
7. hooks 相关硬约束：
   - 写入门禁必须覆盖 Edit / MultiEdit / Write
   - Bash 门禁要支持 implementing 阶段黑名单模式、verifying 阶段白名单模式
   - Windows 优先，command_windows 为权威字段
8. 第一阶段到第二阶段的切换，必须通过 feature.json 中的 phase / phase_gate_approved 等字段控制；不得仅凭会话中的口头确认越级进入 phase2。

====================
二、你的工作边界
====================

你现在只允许处理治理层内容，包括但不限于：
- 审计并重构 .claude/ 治理结构
- 审计 specs/ 的历史执行入口问题
- 设计规范结构
- 设计模板体系
- 设计 Claude Code 落地方式
- 设计自动执行闭环
- 输出目录结构与命名规范
- 输出 rules / hooks / skills / agents 的建议方案
- 创建规范文档、模板文档、说明文档、建议配置

你现在明确禁止：
- 修改任何业务模块
- 借验证流程为由改业务代码
- 直接进入第二阶段增强能力
- 擅自扩展为 deploy / 全自动修复 / 完整 agent 集合 / 大量兼容命令

====================
三、必须遵守的执行顺序
====================

不要一上来直接生成文件。

你必须严格按下面顺序执行，并且每一步都要有明确产出。

--------------------------------
第 1 步：分析痛点与风险
--------------------------------

你必须先解释以下内容，且要结合当前项目场景，不要空谈：

1. 为什么传统 spec 对小白不友好
2. 为什么仅靠“简化文档”仍然不够
3. 为什么必须把“小白版”和“AI 执行版”拆开
4. 为什么必须同时考虑 token、落位、自动闭环
5. 为什么必须区分“说明层”和“生效层”
6. 为什么当前只能做第一阶段，不能直接跳第二阶段
7. 为什么 hooks 验证通过 ≠ 第一阶段整体完成

--------------------------------
第 2 步：正式审计现状
--------------------------------

开始设计前，必须先读取并输出四栏审计结果，格式必须严格为：

- 保留项
- 修正项
- 删除项
- 迁移项

审计范围至少必须覆盖：

- CLAUDE.md
- .claude/settings.json
- .claude/rules/
- .claude/commands/
- .claude/agents/
- specs/README.md
- 现有命令与 agent 中对 specs/<功能名>/ 的路径假设
- 现有命令文案中对 /project:* 的引用
- CLAUDE.local.md
- .claude/settings.local.json
- CLAUDE.md / .claude/CLAUDE.md 中的 @import 引用
- 用户级 ~/.claude/ 配置
- ~/.claude/CLAUDE.md
- ~/.claude/agents/
- ~/.claude/skills/
- 其他已存在或潜在的隐藏规则源

注意：
- 不能只说“已读取”
- 必须给出具体判断
- 必须明确哪些会影响 Claude 实际行为
- 如果发现“说明层”和“生效层”混杂，必须点出来

--------------------------------
第 3 步：提出总体结构
--------------------------------

你必须给出完整但克制的第一阶段总体结构，至少包括：

1. 整套规范的层次设计
2. 小白版与 AI 执行版的职责划分
3. 最小常驻上下文方案
4. Claude Code 识别载体方案
5. 自动执行闭环总体方案
6. 为什么当前采用“最小落地”而不是一次性全做
7. 如何确保第一阶段完成前不误入第二阶段

--------------------------------
第 4 步：定义每份文档 / 规则 / 组件职责
--------------------------------

必须给出下列对象的职责说明：

1. 小白版 4 份模板分别负责什么
   - goal.md
   - plan.md
   - steps.md
   - acceptance.md

2. AI 执行版 4 份模板分别负责什么
   - spec.md
   - design.md
   - tasks.md
   - verify.md

3. 最小 CLAUDE.md 负责什么
4. 核心 rules 放哪里、负责什么
5. gate-write hook 负责什么
6. gate-bash hook 负责什么
7. feature.json 负责什么
8. 最小示例 feature 负责什么
9. command / skill / agent 在第一阶段分别保留什么、迁移什么、暂不做什么

必须明确：
- 哪些是常驻
- 哪些是按需加载
- 哪些是说明层
- 哪些是生效层
- 哪些自动运行
- 哪些只能人工触发

--------------------------------
第 5 步：输出第一阶段缺失交付物清单
--------------------------------

你必须列出：

1. 第一阶段要求的全部交付物清单
2. 其中哪些已经存在
3. 哪些虽然部分存在，但仍不达标
4. 哪些还未创建
5. 当前“还不能进入第二阶段”的真实原因

这里不要泛泛说“还没完成”，要逐项列清楚。

--------------------------------
第 6 步：输出可直接落地的草案
--------------------------------

在不碰业务代码的前提下，你才可以开始输出第一阶段草案。

必须输出的内容包括：

1. 最小 CLAUDE.md 草案
2. 核心 rules 草案
3. 小白版 4 份模板正文
4. AI 执行版 4 份模板正文
5. feature.json 模板正文
6. 最小示例 feature 目录结构与示例正文
7. 迁移说明草案
8. 基本验收清单
9. command / skill / agent 的保留与迁移方案

要求：
- 先给目录结构
- 再给文件职责
- 再给模板正文
- 不要跳步
- 每个文件都要说明“为什么需要它”
- 所有输出都必须服务于第一阶段，不得偷带第二阶段内容

--------------------------------
第 7 步：给出执行落地建议
--------------------------------

你必须给出一份“从当前状态推进到第一阶段可验收状态”的执行建议，包含：

1. 建议先创建哪些文件
2. 建议后创建哪些文件
3. 哪些内容必须先人工审
4. 哪些内容可以 Claude 自动推进
5. hooks、rules、模板之间如何联动
6. 如何用 verify-hooks.ps1 做回归验证
7. 哪些调试残留应清理
8. 哪些历史结构要兼容，哪些要停止继续使用

--------------------------------
第 8 步：输出最终报告
--------------------------------

最后必须输出“第一阶段当前完成度报告”，明确写出：

1. 已完成什么
2. 还缺什么
3. 当前是否达到第一阶段可验收状态
4. 现在还不能进入第二阶段的原因
5. 下一步最合理的动作是什么

并追加一份：
- 非程序员验收说明

====================
四、强制硬约束
====================

你必须严格遵守以下硬约束：

1. 只保留一个执行态目录根：features/<feature-id>/
2. specs/ 不再作为新功能执行入口，只能作为历史兼容或迁移对象审计
3. feature.json 是唯一状态文件格式
4. command 体系只允许最小扁平命令思路，不要恢复 /project:* 作为主入口
5. 高风险 skill 默认 disable-model-invocation: true
6. 写入类 hook 必须覆盖 Edit / MultiEdit / Write，不能遗漏 MultiEdit
7. Windows 优先，command_windows 为权威字段
8. verify 阶段必须检查小白版与 AI 执行版之间是否出现实质分叉
9. 第一阶段默认 phase = phase1
10. 只有在 phase_gate_approved = true 且批准信息完整时，才允许进入 phase2
11. Claude 不得仅凭会话口头确认进入第二阶段
12. 先纠错，再扩展
13. 先分清说明层与生效层，再产出文件
14. 先分清小白版与 AI 执行版，再生成模板
15. 先建立最小可执行闭环，再谈高级自动化
16. 先保证 Windows 可运行和 Claude Code 可识别，再谈抽象设计美观
17. 先保证小白可审查与边界可控，再谈自动化程度
18. 先审计现有结构，再做迁移与重构
19. 先完成第一阶段交付，再进入第二阶段增强

====================
五、输出风格要求
====================

1. 全程中文
2. 不要空话
3. 不要把“分析”和“交付”混在一起
4. 不要只讲概念，不落地
5. 每创建或建议创建一个文件，都要说明职责
6. 如果发现现有结构与约束冲突，先纠错再扩展
7. 若你发现某项前提不足，必须明确指出“不足点是什么、会影响哪一步”，不能假装已经完成
8. 不允许用“已掌握所有材料，现在分析”这种笼统表述代替正式审计结果
9. 不允许把“hooks 已通过验证”误表述为“第一阶段已完成”
10. 不允许跳过四栏审计直接开始写文件
11.如有需要执行的任务，请直接输出到H:\claude_worke\docs\handoff目录下，我会让sonnet模型去读取并按要求执行

现在开始执行。
先从：
1. 痛点与风险分析
2. 四栏审计结果
这两部分开始。