# 阶段需求 01 收口执行报告（校正版）

执行日期：2026-03-28
校正日期：2026-03-28
执行依据：`docs/spec-system/阶段需求/01-现状收敛与口径统一.md`

---

## 说明

本报告按**项目实际已落地内容**校正。  
原报告中“全仓字面清零”与“阶段 01 未触碰 hook 修复”的描述，与当前仓库真实状态不一致，现已改为按实际产出记录。

## 实际修改文件清单

| 文件 | 实际产出 |
|------|----------|
| `CLAUDE.md` | 将执行基线中的旧版嵌套命令表述改写为不含旧字面的禁止表述 |
| `docs/需求.md` | 统一批准字段命名；明确 hooks 运行时位置为用户级配置；执行基线中的旧命令表述已改写 |
| `docs/spec-system/验收清单.md` | 口径收口并改为签收版 |
| `docs/spec-system/验收清单审查报告.md` | 收口为签收版审查结论 |
| `.claude/hooks/gate-bash.js` | 修复 verifying 白名单绕过 |
| `.claude/hooks/gate-write.js` | 补齐 approved/implementing/verifying 批准字段校验 |
| `.claude/rules/governance-core.md` | 与 gate-write.js 的批准字段校验保持一致 |
| `.claude/rules/implementation-gate.md` | 与 gate-write.js 的执行层级标注保持一致 |
| `tests/verify-hooks.ps1` | 从 15 项扩充到 18 项，补入命令拼接绕过用例 |
| `features/example-login/feature.json` | 添加 `demo_only` / `demo_note`，明确占位演示定位 |
| `features/example-login/*.md` | 添加占位演示声明横幅 |

---

## 5 条已收口结论

| # | 口径项 | 已定结论 |
|---|--------|----------|
| 1 | 旧版嵌套命令字面 | **阶段 01 允许修改范围内的执行基线文件**不再出现旧版嵌套命令字面；仓库内其他历史迁移/审计/指令资料可保留旧字面，但不得作为当前执行入口、验收依据或治理事实源 |
| 2 | hooks 注册位置 | 运行时加载位置为用户级 `~/.claude/settings.json`；项目文档仅将其描述为**环境接入条件**；项目级 `.claude/settings.json` 不是 hooks 注册位置 |
| 3 | 批准字段命名 | 唯一合法命名：`approved_by` / `approved_at` / `approved_method` / `approved_note`；禁止 `approval_*` 变体 |
| 4 | example-login 定位 | 正式定位为占位演示示例，不作为真实闭环验收 |
| 5 | 第一阶段占位示例 | 明确允许；真实可运行示例留到后续阶段 |

---

## 本阶段实际并入的最小修复

虽然原始阶段 01 文档把 hook 逻辑修复留给后续阶段，但当前仓库实际已经完成以下最小修复，现统一并入阶段 01 结果：

1. `gate-bash.js` 白名单绕过修复
2. `gate-write.js` 批准字段校验补齐
3. `governance-core.md` / `implementation-gate.md` 与代码实现对齐
4. `tests/verify-hooks.ps1` 补充 3 条绕过回归测试

这些内容已真实落地，不再作为后续阶段的“待修项”重复执行。

---

## 阶段 01 状态

**已完成。**

- 5 个口径问题均已落定到仓库文件
- 阶段 01 执行基线文件中的旧版嵌套命令字面已清零
- 批准字段命名已统一
- `example-login` 定位无歧义
- 最小 gate 修复与回归测试已实际落地
- 后续阶段可直接基于本次结果继续推进

## 下一阶段

下一活跃阶段为：

- `docs/spec-system/阶段需求/02-第一阶段最小闭环修复.md`
