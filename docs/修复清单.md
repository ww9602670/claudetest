  发现的问题（最小修复清单）

  #: P1
  问题: feature.json
    模板和示例缺少需求文档要求的字段：repeat_failure_stop_threshold、human_review_required_when、stop_conditions、eviden
  ce_dir、owner、phase_gate_approved_by、phase_gate_approved_at、phase_gate_note
  严重程度: 中
  修复建议: 补齐这些字段到模板和示例的 feature.json 中
  ────────────────────────────────────────
  #: P2
  问题: gate-write.js 只检查 features/ 下的写入，不检查 features/ 之外但属于 allowed_paths 的业务代码路径（如
    src/auth/）。需求要求所有业务代码写入都须经过门禁
  严重程度: 中
  修复建议: 改为：任何不属于豁免路径（.claude/、docs/、specs/）的写入都须有活跃 feature 且路径在 allowed_paths 内
  ────────────────────────────────────────
  #: P3
  问题: gate-bash.js 在白名单模式下阻断时用 exit 0（第 78 行），应为 exit 2 才能真正阻断 Claude 执行
  严重程度: 高
  修复建议: 第 78 行和第 87 行的 process.exit(0) 改为 process.exit(2)
  ────────────────────────────────────────
  #: P4
  问题: gate-write.js 第 50 行 feature.json 不存在时 allow() 放行，意味着对 features 目录外的业务代码完全没有门禁
  严重程度: 中
  修复建议: 与 P2 合并修复
  ────────────────────────────────────────
  #: P5
  问题: spec-workflow.md 第 25 行仍写"specs 下的功能目录名"，应改为"features 下的"
  严重程度: 低
  修复建议: 文案修正