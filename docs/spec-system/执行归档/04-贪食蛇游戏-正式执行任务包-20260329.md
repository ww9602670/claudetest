# 04 正式执行任务包：网页版贪食蛇游戏

**执行状态**：✅ 已获人工批准，可正式执行  
**执行对象**：网页版贪食蛇游戏  
**执行日期**：2026-03-29  
**批准依据**：《04-实施对象重定位审查-20260329.md》第七章人工批准记录

---

## 一、执行目标

### 1.1 核心目标

围绕"网页版贪食蛇游戏"验证以下机制是否成立：

1. **spec 驱动开发机制**：从需求 → 设计 → 实施任务的完整流程
2. **非程序员可读说明同步产出**：在开发过程中同步产出非技术人员可理解的说明文档
3. **自动化开发与自动修复**：验证 Sonnet 能否基于 spec 自动开发并自动修复问题
4. **evidence 与复审机制**：建立可审计的证据链与复审闭环

### 1.2 验证标准

完成后必须能够回答：

- spec 三件套是否能驱动完整开发流程？
- 非程序员说明是否真的可读且同步？
- 自动修复边界是否清晰且可控？
- evidence 是否完整且可追溯？

---

## 二、执行边界

### 2.1 明确包含

- ✅ 网页版贪食蛇游戏的完整开发流程
- ✅ spec 三件套的建立与驱动
- ✅ feature.json 的状态机推进
- ✅ 非程序员可读说明的产出
- ✅ required_checks 的定义与执行
- ✅ evidence 的收集与归档
- ✅ Git 分支治理的应用
- ✅ CI 最小闭环的建立

### 2.2 明确排除

- ❌ 不扩展到其他业务对象
- ❌ 不扩展到 05 阶段能力（Stop/UserPromptSubmit/完整 fix-loop）
- ❌ 不涉及 deploy/release 能力
- ❌ 不涉及完整 agents/subagents 编排

### 2.3 边界判定原则

当遇到以下情况时，必须停止并记录 blocked：

1. 需要超出贪食蛇游戏范围的能力
2. 需要 05 阶段才应具备的能力
3. 遇到无法通过自动修复解决的阻塞


---

## 三、spec 三件套结构

### 3.1 目录结构

```
features/snake-game/
├── feature.json                    # 状态机与元数据
├── spec/
│   ├── requirements.md            # 需求文档
│   ├── design.md                  # 设计文档
│   └── tasks.md                   # 实施任务清单
├── docs/
│   └── user-guide.md              # 非程序员可读说明
├── src/
│   ├── index.html                 # 游戏页面
│   ├── game.js                    # 游戏逻辑
│   └── style.css                  # 样式
├── evidence/                       # 证据目录
└── README.md                       # 项目说明
```

### 3.2 spec 三件套内容要求

**requirements.md（需求文档）**
- 游戏功能需求（移动、吃食物、碰撞检测、计分）
- 交互需求（键盘控制、重新开始）
- 视觉需求（网格、蛇、食物显示）

**design.md（设计文档）**
- 技术选型（纯 HTML/CSS/JS）
- 数据结构设计（蛇身数组、食物坐标）
- 核心算法设计（移动逻辑、碰撞检测）
- 状态管理设计（游戏状态机）

**tasks.md（实施任务清单）**
- 任务拆解（按优先级）
- 每个任务的验收标准
- 任务间依赖关系


---

## 四、非程序员可读说明产出要求

### 4.1 产出时机

必须在以下节点同步产出：

1. 需求确定后：产出"游戏玩法说明"
2. 设计完成后：产出"游戏功能说明"
3. 实施完成后：产出"使用指南"

### 4.2 内容要求

**必须包含**：
- 游戏是什么（用途、目标）
- 如何开始游戏
- 如何操作（键盘控制说明）
- 游戏规则（如何得分、如何结束）
- 常见问题解答

**禁止包含**：
- 技术实现细节
- 代码片段
- 开发者术语

### 4.3 可读性标准

- 使用日常语言，避免技术术语
- 配图说明（如果需要）
- 分步骤说明操作流程
- 非技术背景人员能够理解并使用


---

## 五、feature.json 结构要求

### 5.1 必需字段

```json
{
  "feature_id": "snake-game",
  "name": "网页版贪食蛇游戏",
  "description": "用于验证 spec 驱动开发机制的贪食蛇游戏",
  "demo_only": false,
  "phase": "phase2",
  "phase_gate_approved": true,
  "phase_gate_approved_by": "用户（人工拍板）",
  "phase_gate_approved_at": "2026-03-29T16:04:00Z",
  "phase_gate_note": "批准作为04首个正式实施对象，验证spec驱动开发机制",
  "status": "planning",
  "required_checks": [],
  "evidence_dir": "evidence",
  "created_at": "2026-03-29T16:16:00Z",
  "updated_at": "2026-03-29T16:16:00Z"
}
```

### 5.2 状态机推进规则

**允许的状态转换**：
- `planning` → `implementing`：spec 三件套完成后
- `implementing` → `verifying`：代码实施完成后
- `verifying` → `done`：所有 required_checks 通过 + CI 通过
- 任意状态 → `blocked`：遇到无法继续的阻塞

**禁止的状态转换**：
- 跳过中间状态
- `done` 后再回退（除非明确需要修复）

### 5.3 done 判定条件

必须同时满足：

1. 所有 required_checks 执行通过
2. CI 检查全部通过
3. evidence 完整且可追溯
4. 非程序员可读说明已产出


---

## 六、真实工程入口定义

### 6.1 required_checks 结构

贪食蛇游戏的 required_checks 必须包含以下检查项：

```json
{
  "required_checks": [
    {
      "id": "check-html-valid",
      "name": "HTML 文件有效性检查",
      "shell": "cmd",
      "command_windows": "type src\index.html",
      "command_bash": "cat src/index.html",
      "cwd": "features/snake-game",
      "timeout_sec": 10,
      "allow_network": false,
      "evidence_file": "evidence/html-check.txt",
      "stop_on_fail": true
    },
    {
      "id": "check-js-syntax",
      "name": "JavaScript 语法检查",
      "shell": "cmd",
      "command_windows": "node --check src\game.js",
      "command_bash": "node --check src/game.js",
      "cwd": "features/snake-game",
      "timeout_sec": 10,
      "allow_network": false,
      "evidence_file": "evidence/js-syntax-check.txt",
      "stop_on_fail": true
    },
    {
      "id": "check-game-start",
      "name": "游戏启动检查",
      "shell": "cmd",
      "command_windows": "node test\smoke-test.js",
      "command_bash": "node test/smoke-test.js",
      "cwd": "features/snake-game",
      "timeout_sec": 30,
      "allow_network": false,
      "evidence_file": "evidence/game-start-check.txt",
      "stop_on_fail": false
    }
  ]
}
```

### 6.2 真实工程动作

贪食蛇游戏的真实工程动作包括：

1. **启动验证**：能否在浏览器中打开
2. **交互验证**：键盘控制是否响应
3. **逻辑验证**：蛇移动、食物生成、碰撞检测是否正常
4. **状态验证**：计分、游戏结束、重新开始是否正常


---

## 七、evidence 要求

### 7.1 必需的 evidence 类型

1. **命令执行证据**：每个 required_checks 的执行输出
2. **状态变更证据**：feature.json 状态变更记录
3. **CI 执行证据**：CI 运行结果与链接
4. **复审证据**：阶段性复审结论

### 7.2 evidence 文件命名规范

```
evidence/
├── html-check.txt                    # HTML 检查输出
├── js-syntax-check.txt               # JS 语法检查输出
├── game-start-check.txt              # 游戏启动检查输出
├── ci-run-{timestamp}.md             # CI 运行记录
├── status-change-{timestamp}.md      # 状态变更记录
└── review-{timestamp}.md             # 复审记录
```

### 7.3 evidence 与 required_checks 映射

每个 required_checks 必须有对应的 evidence 文件，映射关系必须可追溯：

- `check-html-valid` → `evidence/html-check.txt`
- `check-js-syntax` → `evidence/js-syntax-check.txt`
- `check-game-start` → `evidence/game-start-check.txt`


---

## 八、自动修复边界

### 8.1 允许自动修复的范围

Sonnet 可以自动修复以下问题：

1. **语法错误**：HTML/CSS/JS 语法错误
2. **简单逻辑错误**：变量名错误、条件判断错误
3. **样式问题**：CSS 样式调整
4. **文档错误**：拼写错误、格式问题

### 8.2 禁止自动修复的范围

以下情况必须停止并记录 blocked：

1. **需求不明确**：spec 中需求描述不清晰
2. **设计冲突**：实施与设计文档冲突
3. **环境问题**：Node.js 未安装、浏览器不支持
4. **超出范围**：需要后端服务、数据库等

### 8.3 修复次数限制

- 同一问题最多尝试修复 3 次
- 3 次后仍未解决，必须记录 blocked 并停止
- blocked 记录必须包含：问题描述、已尝试方案、建议解决方向


---

## 九、Sonnet 可执行范围

### 9.1 Sonnet 可以独立完成的任务

1. **spec 三件套编写**：基于目标编写 requirements.md、design.md、tasks.md
2. **代码实施**：根据 spec 编写 HTML/CSS/JS 代码
3. **required_checks 执行**：运行检查命令并收集输出
4. **evidence 收集**：将执行结果归档到 evidence 目录
5. **状态推进**：更新 feature.json 状态
6. **简单修复**：修复语法错误、简单逻辑问题
7. **文档产出**：编写非程序员可读说明

### 9.2 Sonnet 需要人工确认的任务

1. **需求变更**：spec 需要重大调整时
2. **blocked 解除**：遇到阻塞需要人工决策
3. **done 判定**：最终完成需要人工复审

### 9.3 Sonnet 禁止执行的任务

1. **跳过检查**：不得跳过 required_checks
2. **伪造证据**：不得编造 evidence
3. **强制推进**：检查失败时不得强制标记 done
4. **超出边界**：不得扩展到其他业务或 05 能力


---

## 十、停止条件

### 10.1 正常完成条件

满足以下所有条件时，判定为正常完成：

1. ✅ spec 三件套完整且一致
2. ✅ 代码实施完成且可运行
3. ✅ 所有 required_checks 通过
4. ✅ CI 检查全部通过
5. ✅ evidence 完整且可追溯
6. ✅ 非程序员可读说明已产出
7. ✅ feature.json 状态为 done

### 10.2 阻塞停止条件

遇到以下任一情况时，必须停止并记录 blocked：

1. ❌ 需求不明确且无法推进
2. ❌ 环境依赖缺失（Node.js、浏览器等）
3. ❌ 同一问题修复 3 次仍未解决
4. ❌ 需要超出边界的能力（后端、数据库等）
5. ❌ 需要 05 阶段才具备的能力

### 10.3 停止后的处理

- 记录 blocked 原因到 evidence
- 更新 feature.json 状态为 blocked
- 产出阻塞报告，说明问题与建议方案
- 等待人工决策


---

## 十一、执行流程

### 11.1 阶段划分

**阶段 1：规划（planning）**
1. 创建 feature 目录结构
2. 编写 spec 三件套
3. 定义 required_checks
4. 编写初始非程序员说明
5. 状态：planning → implementing

**阶段 2：实施（implementing）**
1. 根据 tasks.md 逐个实施任务
2. 编写代码（HTML/CSS/JS）
3. 本地执行 required_checks
4. 收集 evidence
5. 状态：implementing → verifying

**阶段 3：验证（verifying）**
1. 执行所有 required_checks
2. 触发 CI 检查
3. 验证 evidence 完整性
4. 完善非程序员说明
5. 状态：verifying → done 或 blocked

### 11.2 每个阶段的交付物

| 阶段 | 必需交付物 | 验收标准 |
|------|-----------|---------|
| planning | spec 三件套、feature.json、初始说明 | spec 完整一致、结构清晰 |
| implementing | 代码文件、本地检查证据 | 代码可运行、基本功能实现 |
| verifying | 完整 evidence、CI 记录、最终说明 | 所有检查通过、文档完整 |


---

## 十二、Git 分支治理

### 12.1 分支命名规范

- 主分支：`master`
- feature 分支：`feature/snake-game`

### 12.2 提交规范

提交信息格式：
```
<type>: <description>

[optional body]
```

type 类型：
- `feat`: 新功能
- `fix`: 修复问题
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

### 12.3 提交要求

- 每个提交必须有清晰的描述
- 提交必须是原子性的（一个提交只做一件事）
- 提交前必须通过本地 required_checks


---

## 十三、CI 最小闭环

### 13.1 CI 配置要求

创建 `.github/workflows/snake-game-check.yml`，包含以下检查：

1. **HTML 有效性检查**
2. **JavaScript 语法检查**
3. **游戏启动检查**

### 13.2 CI 与 evidence 绑定

每次 CI 运行后，必须：

1. 记录 CI Run ID
2. 记录检查结果（通过/失败）
3. 将结果归档到 evidence 目录
4. 在 feature.json 中关联 CI 记录

### 13.3 CI 触发条件

- 推送到 `feature/snake-game` 分支时触发
- 手动触发（workflow_dispatch）


---

## 十四、验收标准

### 14.1 功能验收

- ✅ 游戏可在浏览器中打开
- ✅ 键盘方向键可控制蛇移动
- ✅ 蛇吃到食物后身体变长
- ✅ 蛇碰到墙壁或自己后游戏结束
- ✅ 计分功能正常
- ✅ 可重新开始游戏

### 14.2 工程验收

- ✅ 所有 required_checks 通过
- ✅ CI 检查全部通过
- ✅ evidence 完整且可追溯
- ✅ Git 提交符合规范
- ✅ 分支治理符合要求

### 14.3 文档验收

- ✅ spec 三件套完整一致
- ✅ 非程序员可读说明清晰易懂
- ✅ README 说明完整
- ✅ 代码注释充分


---

## 十五、完成判定

### 15.1 完成的客观标准

以下条件**全部满足**才可判定完成：

1. ✅ feature.json 状态为 `done`
2. ✅ 所有 required_checks 最后一次执行结果为通过
3. ✅ CI 最后一次运行结果为通过
4. ✅ evidence 目录包含所有必需证据文件
5. ✅ 非程序员可读说明已产出且可读性验证通过
6. ✅ spec 三件套与实际代码一致
7. ✅ Git 提交历史清晰可追溯

### 15.2 不允许判定完成的情况

- ❌ 任何 required_checks 失败
- ❌ CI 检查失败
- ❌ evidence 缺失或不完整
- ❌ 非程序员说明缺失
- ❌ 状态为 blocked


---

## 十六、执行指令（给 Sonnet）

### 16.1 启动指令

当收到"开始执行贪食蛇游戏 04 任务"时，Sonnet 应：

1. 创建 `features/snake-game/` 目录结构
2. 创建初始 feature.json（状态为 planning）
3. 开始编写 spec 三件套

### 16.2 执行原则

- 严格按照 spec 三件套驱动开发
- 每个阶段完成后更新状态
- 及时收集 evidence
- 遇到阻塞立即停止并记录

### 16.3 禁止事项

- 不得跳过 spec 直接写代码
- 不得跳过 required_checks
- 不得伪造 evidence
- 不得超出执行边界


---

## 十七、关联文档

### 17.1 上游依赖文档

- `docs/spec-system/需求.md`
- `docs/spec-system/阶段需求/04-第二阶段工程化接入.md`
- `docs/spec-system/阶段需求/04-启动复审报告-20260329-第二轮.md`
- `docs/spec-system/阶段需求/04-实施对象重定位审查-20260329.md`

### 17.2 参考文档

- `docs/spec-system/evidence目录规范.md`
- `docs/spec-system/hooks验证方案.md`
- `docs/spec-system/第一阶段evidence基线.md`

### 17.3 输出文档

执行完成后应产出：

- `features/snake-game/` 完整目录
- `docs/spec-system/执行归档/04-贪食蛇游戏-执行报告-{timestamp}.md`

---

## 十八、重要声明

### 18.1 本任务包的定位

- ✅ 本任务包是 04 阶段针对"网页版贪食蛇游戏"的正式执行指令
- ✅ 本任务包已获人工批准，可直接交给 Sonnet 执行
- ✅ 本任务包明确了执行边界、停止条件、验收标准

### 18.2 执行限制

- ❌ 仅限"网页版贪食蛇游戏"，不得扩展到其他对象
- ❌ 仅限 04 阶段能力，不得使用 05 阶段能力
- ❌ 遇到阻塞必须停止，不得强行推进

### 18.3 成功标准

本任务包执行成功的标志是：

1. 验证了 spec 驱动开发机制可行
2. 验证了非程序员说明可同步产出
3. 验证了自动修复边界清晰可控
4. 验证了 evidence 与复审机制完整

---

**任务包编制人**：Opus  
**编制日期**：2026-03-29  
**批准依据**：《04-实施对象重定位审查-20260329.md》人工批准记录  
**状态**：✅ 可正式执行  
**执行者**：Sonnet
