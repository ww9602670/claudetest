你现在不要写代码，不要直接推进 04 实施，不要先入为主认为 04 已经通过。
你本次的唯一任务是：

# 基于“E1~E5 已全部关闭、R1~R6 已满足”的最新状态

# 重新执行一次严格的 04 启动复审

# 并且：如果复审通过，直接产出可交给 Sonnet 的 04 正式执行任务包

注意，这次与你之前的几轮任务不同：

* 不是再修 04 文档
* 不是再产出前置补齐任务包
* 不是再解释为什么 04 不能启动
* 而是要基于**最新完成状态**，重新判断：

> **04 现在是否终于具备启动实施条件？**

并在通过时，直接把下一步执行包交出来。

---

# 一、你本次必须使用的依据

请严格以以下材料为依据开展复审，不能脱离这些材料空谈：

## 1. 上位依据

* 总需求文档
* 01 / 02 / 03 / 04 / 05 阶段文档
* 01~03 阶段审查报告、验收清单、evidence 规范、hooks 验证方案、第一阶段 evidence 基线等

## 2. 历史审查与修订链

* 《04 执行前前置条件审查报告》
* 《基于修订后 04 文档集的启动复审报告》
* 《04 启动前真实前提补齐任务包》

## 3. 修订后的 04 文档集

* `04-第二阶段工程化接入.md`
* `04-任务拆解与执行计划.md`
* `04-验收证据模板.md`
* `04-启动前复审清单.md`

## 4. 最新真实前提补齐结果

重点以这些最新结果为准：

* `features/step2-engineering-baseline/feature.json`
* `features/step2-engineering-baseline/evidence/04-precondition-summary-*.md`
* `features/step2-engineering-baseline/evidence/mapping-table-*.md`
* `features/step2-engineering-baseline/evidence/ci-status-*.md`
* `.github/workflows/step2-baseline-check.yml`
* 其他与 E1~E5、R1~R6 关闭状态直接相关的真实 evidence

你必须把“最新 evidence 已显示 E1~E5 全部关闭、R1~R6 全部满足”作为本轮复审的重要输入，但**不能直接无条件采信**，必须复核这些结论是否成立、是否足以支持 04 启动。

---

# 二、你本次要回答的核心问题

你最终必须回答的是：

> **在最新真实前提补齐完成后，04 现在是否已经具备“启动实施”的条件？**

并且要继续回答第二层问题：

> **如果具备启动条件，是否已经足够安全地把 04 正式执行任务交给 Sonnet？**

你必须严格区分以下三种状态：

1. **允许启动 04**
2. **有条件允许启动 04**
3. **不允许启动 04**

不能因为前置补齐已经完成，就自动滑到“允许启动”。

---

# 三、你必须重点复核的内容

## 1. E1~E5 是否真的全部关闭

不要只复述 summary，要重新判断：

* E1：真实 feature 是否真实成立，而非仅形式创建目录
* E2：phase2 批准是否已真实、完整、边界清晰
* E3：真实工程入口与真实执行证据是否成立
* E4：CI 基线与运行证据是否成立
* E5：`required_checks -> CI -> evidence` 映射是否真实、可追溯、可复核

如果其中任何一项只是“形式满足”而非“可依赖满足”，必须明确指出。

---

## 2. R1~R6 是否真的全部满足

不要只看文档勾选，要判断：

* 这些条件是否满足了“下一步允许 04 启动”的真实门槛
* 还是只满足了“前置补齐阶段的自我闭环”

也就是说，要防止“前置补齐完成”被错误偷换成“04 可以实施”。

---

## 3. 当前 04 是否适合交给 Sonnet 正式执行

你必须单独判断：

> **现在把 04 正式实施任务交给 Sonnet，是否安全？**

重点判断：

* Sonnet 还会不会被迫继续补规则
* Sonnet 还会不会因为前提不稳而进入伪执行
* 04 与 05 边界是否已足够稳
* 当前 evidence 是否足够支撑实施导向任务，而不是继续文档动作

---

# 四、复审后的分流规则

## 分流 A：若结论为“允许启动 04”

你必须继续输出：

### A1. 允许启动依据

明确说明：

* 为什么现在允许启动
* 哪些前提已经真实成立
* 哪些风险已降到可接受范围
* 为什么现在交给 Sonnet 是安全的

### A2. 给 Sonnet 的 04 正式执行任务包

你必须直接产出一份**实施导向**任务包，而不是再修文档。

任务包必须至少写清：

1. 执行目标
2. 执行边界
3. 可修改文件范围
4. 禁止修改范围
5. 任务顺序
6. 每任务完成标准
7. 每任务必需证据
8. 失败/停止条件
9. 哪些地方仍需人工确认
10. 明确 Sonnet 是执行者，不是规则重写者

### A3. 启动后限制

必须明确：

* 虽允许启动，但哪些事情仍不能做
* 哪些行为会导致立即停止
* 如何防止 04 执行过程污染 05

---

## 分流 B：若结论为“有条件允许启动 04”

你必须继续输出：

### B1. 条件成立项 / 未成立项

把当前状态拆清楚。

### B2. 启动前最后补齐项

用表格列出：

| 优先级 | 补齐项 | 当前缺口 | 是否阻塞启动 | 完成标准 | 完成后如何确认 |

### B3. 可先交 Sonnet 的任务 / 不可交 Sonnet 的任务

必须切清边界，不能模糊。

---

## 分流 C：若结论为“不允许启动 04”

你必须继续输出：

### C1. 不允许启动依据

说明为什么前置补齐完成后仍然不够。

### C2. 剩余真实缺口

明确区分：

* 文档问题
* 事实问题
* 证据问题
* 工程问题

### C3. 禁止空转说明

必须明确写出：

* 不要继续润色文档
* 不要让 Sonnet进入 04 正式实施
* 下一步真正该补什么

---

# 五、你本次必须严格按以下结构输出

# 基于 E1~E5 全部关闭、R1~R6 满足后的 04 启动复审报告

## 一、最终结论

只能三选一：

* 允许启动 04
* 有条件允许启动 04
* 不允许启动 04

并给出 3~8 条核心理由。

---

## 二、复审依据

按类别列出：

* 总需求
* 01~03 阶段材料
* 04 历史审查链
* 修订后的 04 文档集
* 前置补齐任务包
* 最新 evidence 与真实运行结果
* 05 阶段文档

---

## 三、E1~E5 关闭状态复核

请用表格：

| 阻塞项 | 申报状态 | 本轮复核结论 | 依据 | 是否足以支撑 04 启动 |
| --- | ---- | ------ | -- | ------------ |

---

## 四、R1~R6 满足状态复核

请用表格：

| 条件 | 申报状态 | 本轮复核结论 | 依据 | 是否足以支撑实施 |
| -- | ---- | ------ | -- | -------- |

---

## 五、当前是否适合交给 Sonnet 正式执行

这一节必须单独写，明确回答：

* 现在交给 Sonnet 是否安全
* Sonnet 是否还会被迫补前提
* 是否还存在伪执行风险
* 当前是否已经进入“实施导向”阶段

---

## 六、最终分流输出

根据结论只输出以下三种情况之一：

### 情况 A：允许启动 04

输出：

1. 允许启动依据
2. 给 Sonnet 的正式执行任务包
3. 启动后限制与停止条件

### 情况 B：有条件允许启动 04

输出：

1. 条件成立项 / 未成立项
2. 启动前最后补齐项
3. 可先交 Sonnet 的任务 / 不可交 Sonnet 的任务

### 情况 C：不允许启动 04

输出：

1. 剩余阻塞项
2. 下一步应补的真实缺口
3. 明确禁止继续空转

---

## 七、最短行动建议

最后只给一个最明确的下一步建议。

---

# 六、你必须遵守的限制

1. **不要为了推进而放宽标准**
2. **不要因为 summary 声称已关闭，就直接默认真实成立**
3. **不要把“前置补齐完成”自动偷换成“04 可以实施”**
4. **如果结论是不允许启动，不要产出 Sonnet 正式执行任务包**
5. **如果结论是有条件允许，必须明确哪些能做、哪些不能做**
6. **如果结论是允许启动，任务包必须是实施导向，不是继续修文档导向**
7. **必须站在“避免 Sonnet 伪执行、避免后续返工、避免污染 05 边界”的角度审查**
8. **不要继续泛泛润色文档，除非它仍是明确阻塞项**

---

# 七、你的最终任务目标

你这次不是来继续修文档，也不是继续补前提。
你这次是来给出一个**可决策、可分流、可直接进入下一阶段**的结论：

* 能启动，就直接给 Sonnet 正式执行任务包
* 不能启动，就明确卡点
* 有条件启动，就把边界切清楚

现在开始复审。
