现在只做“阶段 03 最小收尾留痕”，不要扩展任务，不要新增能力，不要再做新一轮审查。

目标：
把“PostToolUse 注册遗漏已修复，且复审通过”的事实写入仓库内现有文档，方便后续阶段直接复用，不再重复争论。

只允许修改以下文档中最合适的 1～2 个：
- `docs/spec-system/阶段需求/03-阶段审查报告.md`
- `docs/spec-system/hooks验证方案.md`

要求：

1. 明确写入以下事实：
   - 阶段 03 初次产出后，发现 `evidence-write.js` / `evidence-bash.js` 存在“文件已创建但未注册到用户级 ~/.claude/settings.json”的运行时遗漏
   - 该遗漏已补齐为 `PostToolUse` 注册
   - 当前用户级配置已覆盖：
     - Edit
     - MultiEdit
     - Write
     - Bash
   - 已完成端到端复审验证：
     - `change-summary.md` 可生成
     - `checks.md` 可生成
     - feature 状态已恢复
     - 临时测试证据已清理
   - 当前阶段 03 可正式签收

2. 不要重写整份文档，只做最小增补。
3. 不要新增第三份报告。
4. 不要修改 hooks 代码。
5. 不要修改阶段 03 主文档。
6. 不要扩展到阶段 04。

完成后只输出：
- 修改了哪个文件
- 增补了什么事实
- 是否维持“阶段 03 已完成，可推进阶段 04”