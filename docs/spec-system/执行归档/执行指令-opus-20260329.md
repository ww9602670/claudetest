你现在不要推进 04 实施，不要写代码，不要产出给 Sonnet 的 04 实施任务包。
因为根据上一轮《基于修订后 04 文档集的启动复审报告》，当前结论已经明确：

> **04 仍然不允许启动。当前主阻塞已不再是文档问题，而是“真实前提 / 真实证据 / 真实环境”未成立。**

所以你这次的唯一任务是：

# 产出《04 启动前真实前提补齐任务包》

注意，这个任务包的目标不是“执行 04”，而是：

> **把当前阻塞 04 启动的现实前提逐项补齐，使 04 具备下一次启动复审的现实基础。**

---

# 一、你本次工作的性质

你现在做的不是：

* 继续润色 04 文档
* 继续重复 04 启动复审
* 提前让 Sonnet 进入 04 实施
* 用假设替代真实前提

你现在要做的是：

> **把上一轮启动复审已经确认的“事实阻塞项”，整理成一个清晰、可执行、可验证、可交付的前置补齐任务包。**

这个任务包后续可以交给执行模型（如 Sonnet）去做，但前提是你必须先把范围、顺序、证据要求、停止条件定义清楚，避免执行者进入“边做边猜、边补边定义”的伪执行状态。

---

# 二、你必须依据的材料

请严格基于以下材料工作：

## 1. 上位依据

* 总需求文档
* 01 / 02 / 03 / 04 / 05 阶段文档
* 01~03 审查报告、evidence 规范、hooks 验证方案、第一阶段 evidence 基线等

## 2. 与 04 直接相关的材料

* 《04 执行前前置条件审查报告》
* 《基于修订后 04 文档集的启动复审报告》
* 修订后的 04 文档集：

  * `04-第二阶段工程化接入.md`
  * `04-任务拆解与执行计划.md`
  * `04-验收证据模板.md`
  * `04-启动前复审清单.md`

你必须以“启动复审报告已经确认的事实阻塞项”为直接输入，不能重新回到泛泛文档讨论。

---

# 三、你这次要解决的核心问题

你要围绕下面这件事展开：

> **既然 04 当前不允许启动，那么到底还差哪些“真实前提”必须先补齐？这些前提要如何被补、由谁补、补到什么程度、拿什么证据证明已经补齐？**

也就是说，你必须把“当前不能启动 04”的结论，转换成一套：

* 可分解
* 可执行
* 可验证
* 可再次复审

的**前置补齐任务包**。

---

# 四、你必须以这些“事实阻塞项”为任务源头

根据上一轮启动复审结论，当前主阻塞至少包括：

1. 无非 `demo_only` 的真实 feature 作为 04 工程化接入对象
2. 无真实 `phase2` 启动批准证据
3. 无真实工程入口及一次真实执行输出证据
4. 无 CI 基线与运行证据
5. 无 `required_checks -> CI -> evidence` 的真实链路证据

你必须以这些阻塞项为核心，构造任务包。
不能把任务包写成抽象建议，也不能把这些阻塞项重新转回“文档应该怎么写”。

---

# 五、你本次必须产出的成果

你必须输出一份完整的：

# 《04 启动前真实前提补齐任务包》

该任务包至少必须包含以下部分。

---

## 第一部分：任务包总说明

必须明确写出：

1. 这不是 04 实施任务包
2. 这是一份“04 启动前补齐真实前提”的任务包
3. 目标是让 04 达到“可再次发起启动复审”的状态
4. 当前不能把 04 实施直接交给 Sonnet 的原因是什么
5. 执行这些前置补齐任务后，下一步应该是什么

---

## 第二部分：阻塞项总表

请用表格列出当前阻塞 04 启动的事实项：

| 编号 | 阻塞项 | 当前状态 | 为什么阻塞 04 | 是否必须先补齐 | 补齐后如何验证 |

要求：

* 不能写成空话
* 必须直接对应上一轮启动复审的阻塞结论
* 必须把“文档问题”与“事实问题”区分开
* 当前主阻塞如已不是文档问题，要明确写出

---

## 第三部分：前置补齐任务清单

这是本次最核心的部分。
你必须把“事实阻塞项”拆成**真正可执行的任务**，而不是建议项。

每个任务必须至少写清：

* 任务编号
* 任务名称
* 任务目标
* 为什么这个任务是启动前必做
* 修改/创建对象
* 输入
* 输出
* 依赖关系
* 执行步骤
* 完成标准
* 验证方式
* 必需证据
* 失败/阻塞判定
* 是否允许交给 Sonnet 执行

任务建议至少覆盖以下方向，但你可以按更合理结构重组：

### P0 级任务（应优先）

1. 选定一个真实 feature（非 demo_only）
2. 为该真实 feature 建立或补齐可追溯的 `feature.json`
3. 完成 `phase2` 真实批准入档
4. 确认并记录最小工程入口
5. 跑通一次真实命令并保存输出证据
6. 建立最小 CI 检查链
7. 建立第一份 `required_checks.id -> CI -> evidence_file` 真实映射

你可以调整编号和顺序，但不能弱化这些核心内容。

---

## 第四部分：任务依赖与执行顺序

请明确说明：

* 哪些任务必须先做
* 哪些任务可以并行
* 哪些任务完成前，绝对不能进入下一项
* 哪些任务失败后，必须整体停止

你必须给出类似“依赖链 / 里程碑”的表达，而不是散点建议。

---

## 第五部分：哪些任务可以交给 Sonnet，哪些不可以

这部分必须单独写，不能省略。

因为这次任务包是为了后续执行准备的，所以你必须清楚分流：

### A. 可以交给 Sonnet 的任务

例如：

* 建立目录
* 补 feature.json 字段
* 补证据模板实例
* 接入最小 CI 配置
* 生成结构化映射文件
* 运行明确指定的最小命令并保存输出

### B. 不应交给 Sonnet 单独决定的任务

例如：

* 决定哪个 feature 才算真实 feature（若需要业务方拍板）
* 判定某个批准是否属于真实 phase2 批准
* 放宽启动标准
* 擅自替代人工审批
* 在缺少真实环境时伪造运行证据

你必须写清：

* Sonnet 可以执行什么
* Sonnet 不可以决定什么
* 遇到什么情况 Sonnet 必须停止并上报

---

## 第六部分：执行边界与禁止事项

你必须明确写出这次补齐任务的边界，包括：

1. 这是“启动前补齐”，不是“04 实施”
2. 不能借补齐任务偷偷推进 04 正式范围
3. 不能为了形成闭环而伪造 feature / 批准 / CI / 证据
4. 不能让 Sonnet 一边执行一边重写规则
5. 不能污染 05 的边界
6. 不能把“尚未验证”的前提写成“已成立”

---

## 第七部分：验收与再次复审触发条件

你必须明确写出：

> 执行完这份“真实前提补齐任务包”之后，满足什么条件，才允许再次发起“04 启动复审”？

请至少列出：

* 必须具备哪些新证据
* 哪些阻塞项必须全部关闭
* 哪些可以留待 04 实施再处理
* 再次复审时应该检查什么
* 再次复审的结论只允许是什么范围

也就是说，你要把这份任务包和“下一次启动复审”衔接起来。

---

## 第八部分：最终交付格式

你必须把最终输出整理成下面结构：

# 04 启动前真实前提补齐任务包

## 一、任务包定位与目标

## 二、当前阻塞项总表

## 三、前置补齐任务清单

## 四、任务依赖与执行顺序

## 五、Sonnet 可执行 / 不可执行边界

## 六、执行边界与禁止事项

## 七、完成后的再次复审触发条件

## 八、最短行动建议

---

# 六、你必须遵守的关键限制

1. **不要把这份任务包写成 04 实施任务包**
2. **不要继续泛泛修文档**
3. **不要把阻塞项写成建议项**
4. **必须把阻塞项转成可执行任务**
5. **必须明确哪些可交 Sonnet，哪些不能交**
6. **没有真实证据的事项，不能写成已成立**
7. **不能让 Sonnet 去做需要人工授权/人工判断拍板的事情**
8. **必须防止执行者进入“边做边补前提”的伪执行**
9. **必须服务于下一次“04 启动复审”**
10. **最终输出必须足够具体，能直接作为下一步执行蓝本**

---

# 七、你的最终任务目标

你这次不是来重复说明“为什么 04 不能启动”，
而是要把这个结论推进一步，变成：

> **既然 04 还不能启动，那么现在应该先做什么，做到什么程度，交给谁做，如何验证做完了。**

也就是：

* 把阻塞项任务化
* 把任务执行边界说清
* 把 Sonnet 可做与不可做切开
* 把下一次复审入口准备好

现在开始输出完整任务包。
