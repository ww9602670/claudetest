你这次上一条回复**方向正确，但交付物不合格**。
原因不是你的判断错了，而是你**答非所问**：

* 你再次产出了一份《阶段 04 启动复审报告》
* 但我这次要求你的交付物是：**《04 启动前真实前提补齐任务包》**

也就是说：

> **“04 仍不能启动”的结论，你已经说清楚了；现在不要再重复复审结论，而要把这些已确认的事实阻塞项，直接转成可执行的前置补齐任务包。**

---

# 一、先确认你这次必须纠正的错误

你上一条回复里，已经再次确认了这些事实阻塞项：

1. 无非 `demo_only` 的真实 feature 作为 04 工程化接入对象
2. 无真实 `phase2` 启动批准证据
3. 无真实工程入口及一次真实执行输出证据
4. 无 CI 基线与运行证据
5. 无 `required_checks -> CI -> evidence` 的真实链路证据

这些判断我认可。
**但这些内容现在不能再以“复审报告”的形式重复输出。**

你这次必须做的是：

> **基于这些已经确认的阻塞项，直接产出“如何补齐这些阻塞项”的任务包。**

---

# 二、你现在不要做的事

这次严禁你再次输出以下内容：

1. 不要再写“最终结论：不允许启动 04”
2. 不要再重复“文档层阻塞已关闭、现实前提未成立”
3. 不要再重写一遍“是否适合交给 Sonnet 执行”的论证
4. 不要再输出另一版启动复审报告
5. 不要再泛泛解释为什么 04 还不能启动
6. 不要再把事实阻塞项写成说明性段落

这些内容在上一条里已经足够了。
这次你的任务不是“再判断一次”，而是“把已经判断清楚的阻塞项任务化”。

---

# 三、你这次唯一要交付的东西

你这次只能交付一个东西：

# 《04 启动前真实前提补齐任务包》

这个任务包的目标不是执行 04，
而是：

> **把当前阻塞 04 启动的现实前提逐项补齐，使 04 具备下一次启动复审的现实基础。**

请记住：

* 这不是复审报告
* 这不是 04 实施任务包
* 这不是文档润色建议
* 这必须是一份**任务化、可执行、可验证、可分流给 Sonnet 的前置补齐任务包**

---

# 四、你必须直接使用的输入，不要再重复审查

你必须把下面内容当作已确认事实，直接进入任务包设计：

## 已确认的主阻塞项（直接转任务）

* E1：无非 `demo_only` 的真实 feature
* E2：无真实 `phase2` 启动批准证据
* E3：无真实工程入口及一次真实执行输出证据
* E4：无 CI 基线与运行证据
* E5：无 `required_checks -> CI -> evidence` 的真实链路证据

## 已确认的边界

* 文档层主阻塞项已基本关闭
* 当前不应让 Sonnet 直接执行 04 实施
* 当前不应继续泛泛润色 04 文档
* 下一步应补齐真实前提与真实证据

这些都视为已定事实。
**不要再花篇幅证明它们。**

---

# 五、你这次必须按这个结构输出，不能偏离

你的最终输出必须严格使用下面结构：

# 04 启动前真实前提补齐任务包

## 一、任务包定位与目标

这里要说明：

* 这不是 04 实施任务包
* 这是“04 启动前补齐真实前提”的任务包
* 执行完成后，目标是让 04 达到“可再次发起启动复审”的状态

---

## 二、当前阻塞项总表

必须使用表格：

| 编号 | 阻塞项 | 当前状态 | 为什么阻塞 04 | 是否必须先补齐 | 补齐后如何验证 |

注意：

* 这里写事实，不写空话
* 要直接对应 E1~E5
* 不能再写成长篇复审说明

---

## 三、前置补齐任务清单

这是最核心部分。

你必须把 E1~E5 直接拆成真正可执行的任务。
每个任务都必须写清：

* 任务编号
* 任务名称
* 对应阻塞项
* 任务目标
* 为什么这是启动前必做
* 修改/创建对象
* 输入
* 输出
* 依赖关系
* 执行步骤
* 完成标准
* 验证方式
* 必需证据
* 失败/阻塞判定
* 是否允许交给 Sonnet 执行

注意：

* 不能写成建议
* 必须写成任务
* 必须足够具体，后续可直接执行

至少要覆盖这些任务方向：

1. 选定真实 feature
2. 建立/补齐真实 feature 的 `feature.json`
3. 完成真实 `phase2` 批准入档
4. 确认最小工程入口
5. 跑通一次真实命令并留证
6. 建立最小 CI 检查链
7. 产出第一份 `required_checks.id -> CI -> evidence_file` 真实映射

你可以调整任务结构，但不能缺这些核心任务。

---

## 四、任务依赖与执行顺序

必须写清：

* 哪些先做
* 哪些可并行
* 哪些失败后必须整体停止
* 哪些完成后才能触发下一阶段

你必须给出明确依赖链和里程碑，不要散点描述。

---

## 五、Sonnet 可执行 / 不可执行边界

这部分必须单独写。

### A. 可交给 Sonnet 的任务

要明确列出哪些前置补齐任务可以交 Sonnet 执行。

### B. 不应由 Sonnet 单独决定的事项

要明确列出哪些需要人工拍板/人工确认/人工授权。

### C. Sonnet 的停止条件

要明确 Sonnet 在什么情况下必须停止并上报。

注意：

* 这不是问“Sonnet 能不能做 04”
* 而是问“Sonnet 能不能做这份前置补齐任务包中的哪些任务”

---

## 六、执行边界与禁止事项

必须明确：

* 这是启动前补齐，不是 04 实施
* 不得借补齐任务偷偷推进 04 正式范围
* 不得伪造 feature / 批准 / CI / 证据
* 不得让 Sonnet 一边执行一边重写规则
* 不得污染 05 边界
* 不得把“未验证前提”写成“已成立”

---

## 七、完成后的再次复审触发条件

必须明确写出：

> 执行完这份任务包后，满足什么条件，才允许再次发起“04 启动复审”？

至少要包括：

* 必须新增哪些证据
* 哪些阻塞项必须全部关闭
* 再次复审时要重点检查什么
* 哪些事项仍然不能提前视为已完成

---

## 八、最短行动建议

最后只给一个最明确、最可执行的下一步建议。

---

# 六、你必须遵守的硬限制

1. **不要再输出复审报告**
2. **不要再重复“不允许启动 04”的论证**
3. **不要再泛泛描述阻塞项**
4. **必须把阻塞项转成可执行任务**
5. **必须明确哪些任务可交 Sonnet，哪些不能**
6. **不能把这份任务包写成 04 实施任务包**
7. **没有真实证据的事项，不能写成已成立**
8. **不能让 Sonnet 做需要人工审批/人工拍板的决定**
9. **最终输出必须可直接作为下一步执行蓝本**
10. **如果你输出的仍是“报告”，而不是“任务包”，就算本次任务失败**

---

# 七、这次你要达成的最终结果

你这次不是再证明“为什么还不能启动 04”，
而是要做到：

> **既然 04 还不能启动，那么现在就把“启动前必须补齐的真实前提”拆成一份完整、可执行、可验证的任务包。**

现在开始直接输出《04 启动前真实前提补齐任务包》，不要再输出复审报告。
