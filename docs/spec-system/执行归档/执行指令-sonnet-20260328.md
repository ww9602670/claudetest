你现在不要碰阶段 02 主线治理逻辑，也不要修改 hooks、skills、agents、feature.json、业务代码。
暂时先不要移动H:\claude_worke\docs\执行指令-opus.md跟H:\claude_worke\docs\执行指令-opus.md
当前这两份文档正在被读取和执行，其他都处理完成后，到最后再询问我是否可以处理这两份文档。
你只做一个辅助任务：

为当前项目起草一份“文档归档、保留、清理、失效判定规则”，用于解决以下问题：
- 项目开发过程中产出大量文档
- 文档类型混杂：执行指令、阶段需求、阶段审查报告、修复报告、复验报告、收口报告、验收清单、临时诊断材料
- 有些文档应长期保留
- 有些文档属于阶段归档
- 有些文档用完即可清理
- 如果不分层，后续模型容易误把历史材料当成当前执行基线

你必须只读取以下范围：

- `docs/需求.md`
- `docs/spec-system/阶段需求索引.md`
- `docs/spec-system/阶段需求/*.md`
- `docs/spec-system/验收清单.md`
- `docs/spec-system/验收清单审查报告.md`
- `docs/spec-system/需求实现现状与阶段拆分报告.md`
- `docs/收口执行报告.md`（如果存在）
- 以及 `docs/` 下当前已存在的阶段报告、执行报告、审查报告类文档

你的任务不是整理历史内容本身，而是输出一份**可落地的文档生命周期规则草案**。

你必须产出一份短文档，建议文件名类似：

- `docs/spec-system/文档生命周期与归档规则.md`

内容必须至少覆盖：

1. 文档分类体系
   至少分成以下 4 类：
   - 当前权威基线文档
   - 阶段归档文档
   - 临时执行文档
   - 废弃 / 历史参考文档

2. 每类文档的保留策略
   - 必须长期保留
   - 可归档保留
   - 用后可删
   - 不得再作为当前执行依据

3. 当前执行优先级规则
   必须明确：
   - Claude 后续执行时默认优先读哪些文档
   - 哪些文档即使存在，也不应作为当前执行基线
   - 如何避免“旧审查报告覆盖新结论”

4. 目录建议
   至少给出一个低复杂度方案，例如：
   - `docs/spec-system/阶段需求/`
   - `docs/spec-system/审查归档/`
   - `docs/spec-system/执行归档/`
   - `docs/spec-system/临时材料/`
   但不要大规模改文件，只做规则草案

5. 文件命名建议
   要让非程序员能一眼看出：
   - 当前有效
   - 历史归档
   - 临时材料
   - 已失效

6. 失效判定规则
   例如：
   - 已被新版本阶段审查报告覆盖
   - 已被收口执行结果吸收
   - 已不再作为后续阶段输入
   - 只保留审计价值，不保留执行价值

7. 与阶段文档的关系
   必须明确：
   - 阶段需求文档属于当前执行主入口
   - 阶段审查报告属于阶段完成判定依据
   - 临时执行指令文档不应长期混入主执行层

严格禁止：

- 修改 hooks
- 修改 `.claude/skills/**`
- 修改 `.claude/agents/*`
- 修改 `feature.json`
- 修改任何业务代码
- 帮项目自动做“清理删除”
- 直接移动文件
- 擅自创建 agent / subagent
- 产出长篇空泛方法论

输出要求：

一、先给一个很短的计划，说明：
- 你会生成哪份规则文档
- 这份文档解决什么问题
- 你不会碰哪些执行主线文件

二、然后直接落文件。

三、最终只输出：
- 新增或修改了哪个文档
- 这份文档的用途
- 文档分类规则摘要
- 归档 / 保留 / 清理的最小建议

补充要求：
- 全程中文
- 面向非程序员可读
- 规则必须简单、能执行
- 不要试图一次做成复杂 agent 体系
- 这只是“先立规则”，不是“立刻自动归档执行器”