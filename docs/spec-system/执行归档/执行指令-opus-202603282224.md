你现在不要写代码，不要推进开发，不要修改文档内容。
你的唯一任务是：**对“04 阶段是否具备执行前置条件”进行一次严格的审查**，并输出一份可作为“是否允许启动 04”的审查结论。

## 审查范围

请以我本次提供/已上传的文档为准，重点审查以下几类材料：

1. **总需求文档**
2. **01 / 02 / 03 阶段文档**
   包括但不限于：requirements、design、tasks、执行报告、自测报告、审查报告、验收说明等
3. **04 阶段文档**
   包括但不限于：requirements、design、tasks、执行计划、阶段目标说明
4. **05 阶段文档**
   用于交叉判断 04 的边界是否合理，是否把本应在 04 完成的内容错误后移到了 05

如果你发现仅靠 04 文档本身无法判断是否可执行，**必须主动回看 01~03 与总需求文档进行交叉比对**，不能只看 04 自己写得是否通顺。

---

## 你的核心任务

你要回答的不是“04 文档写得好不好”，而是以下更关键的问题：

### 一、04 是否真的已经具备执行前提

请严格审查 04 是否已经满足以下启动条件：

1. **上位约束是否清楚**

   * 04 的目标、边界、输入、输出、完成标准，是否与总需求一致
   * 是否存在表述模糊、边界漂移、偷换目标、缩小范围但未声明的情况

2. **前序阶段是否真的已完成到足够支撑 04**

   * 01 / 02 / 03 是否已经把 04 依赖的基础能力、结构、验证链路、前提结论落实到位
   * 是否只是“文档上写完成了”，但实际上缺少证据、缺少验证、缺少可依赖结果
   * 是否有“理论完成、表面完成、未实证完成”的部分被当成 04 的前置条件使用

3. **04 所依赖的未决问题是否已清空**

   * 01~03 是否还遗留关键缺口、风险点、未验证项、伪完成项
   * 这些问题会不会直接导致 04 一启动就返工
   * 04 文档里是否故意或无意跳过了这些未决问题

4. **04 的任务拆解是否可执行**

   * tasks 是否是可落地的执行任务，而不是口号式条目
   * 每个任务是否具备明确对象、修改范围、输入输出、验证方式、完成标准
   * 是否存在依赖顺序错误、循环依赖、任务颗粒度失衡、无法验收的问题

5. **04 的验收条件是否真实可验**

   * 是否存在“写了验收标准，但无法客观验证”
   * 是否依赖主观判断
   * 是否缺少证据链、测试链、真实环境验证或必要的回归验证

---

## 必须重点审查的内容

请特别盯住下面这些问题，不能略过：

### 1. 对总需求的覆盖关系

请明确判断：

* 04 当前计划覆盖了总需求中的哪些部分
* 哪些部分是建立在 01~03 已完成的前提上
* 哪些部分其实 01~03 没有真正完成，却被默认视为已完成
* 哪些本该在 04 处理，但被漏掉了或被推给 05 了

### 2. 01~03 的“完成”是否真实成立

不要只看勾选状态或结论文字。
请审查：

* 是否有实际证据支撑
* 是否有真实验证
* 是否有与总需求逐项对应
* 是否存在“任务完成 ≠ 能力真正落地”的情况

### 3. 04 与 05 的边界是否合理

请判断：

* 04 当前范围是否过窄，导致关键内容被滞后到 05
* 05 是否承担了本应在 04 完成的前置性内容
* 如果 04 按当前文档执行完，是否会出现“看似完成，但整体仍无法继续”的问题

### 4. 是否存在“现在不该推进 04，而应先修文档/补前置缺口”

如果你判断 04 当前还不能执行，必须明确指出：

* 卡点是什么
* 属于哪一类问题（总需求对齐问题 / 前序阶段未闭环 / 04 任务拆解问题 / 验收不可执行 / 边界错位）
* 应先修哪份文档
* 修到什么标准后，04 才可以启动

---

## 输出要求

请严格按以下结构输出，不要省略：

# 04 执行前前置条件审查报告

## 一、结论摘要

只允许给出以下三种结论之一，并说明原因：

* **允许执行 04**
* **有条件允许执行 04**
* **不允许执行 04，必须先补前置条件**

这一部分要先给总判断，再给 3~8 条最核心理由。

---

## 二、依据文档清单

列出你本次审查实际参考了哪些文档，并按类别归类：

* 总需求
* 01 阶段
* 02 阶段
* 03 阶段
* 04 阶段
* 05 阶段

如果某类文档缺失，也要明确写出“未见到，因此该部分判断存在不确定性”。

---

## 三、04 启动前置条件逐项审查

请按下面格式逐项审查，每项都要给出：

* 审查结论：通过 / 部分通过 / 不通过
* 依据
* 风险
* 是否阻塞 04 启动

审查项至少包括：

1. 与总需求的一致性
2. 01~03 对 04 的支撑是否成立
3. 关键依赖是否闭环
4. 任务拆解是否可执行
5. 验收标准是否可验证
6. 04 与 05 的边界是否合理
7. 是否存在重大遗漏或伪完成前提

---

## 四、01~03 对 04 的前置支撑缺口清单

这里不要泛泛而谈，要列成清单表格：

| 编号 | 对应总需求/能力点 | 理应由哪一阶段完成 | 当前状态 | 问题性质 | 是否影响 04 | 说明 |
| -- | --------- | --------- | ---- | ---- | ------- | -- |

重点指出：

* 哪些只是“文档完成”但不是“能力完成”
* 哪些缺少证据
* 哪些缺少验证
* 哪些会导致 04 开始后返工

---

## 五、04 文档本身的问题清单

请列出 04 当前 requirements / design / tasks 中存在的问题，按严重程度分类：

### A. 阻塞执行的问题

### B. 不阻塞但必须尽快修正的问题

### C. 建议优化的问题

每个问题都要写清：

* 出现在哪份文档
* 具体是什么问题
* 会造成什么后果
* 建议如何修正

---

## 六、04 与 05 的边界复核

请明确回答以下问题：

1. 04 当前范围是否完整合理？
2. 有哪些内容本应属于 04，却被遗漏或后移到 05？
3. 有哪些内容放在 05 是合理的？
4. 如果按当前 04 文档执行完，是否足以自然衔接 05？
5. 04+05 执行完后，理论上是否可覆盖总需求预期？如果不能，差在哪？

---

## 七、是否允许启动 04 的最终判断

这一部分必须明确写成：

### 最终判断

* 结论：
* 原因：
* 是否允许马上执行：
* 若不允许，必须先完成的修正项：

### 启动前必须补齐的清单

请给出明确列表，格式如下：

| 优先级 | 需补齐项 | 应修改的文档 | 修改目标 | 完成后如何确认可启动 |
| --- | ---- | ------ | ---- | ---------- |

---

## 八、给执行者的直接建议

最后给出明确建议，只能二选一：

* **建议立即推进 04**
* **建议暂停 04，先修文档/补前置缺口**

如果是“暂停”，请直接给出“下一步先做什么”，不要空泛。

---

## 审查原则

你必须遵守以下原则：

1. **宁可严格，不可放水**
2. **不能因为文档写得完整就默认能力已落地**
3. **不能因为 tasks 已勾选就默认前置条件已真实完成**
4. **必须站在“避免错误启动 04、避免后续返工”的角度审查**
5. **如果证据不足，要明确写“证据不足，不能判定为已完成”**
6. **你的任务是审查，不是代替我修文档**
7. **不要输出模糊结论，必须明确给出能否执行 04**

开始审查。
