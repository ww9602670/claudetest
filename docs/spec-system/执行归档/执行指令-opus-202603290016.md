你现在不要推进 04 开发实现，不要写代码，不要提前进入执行。
你当前唯一任务是：

**根据本次已经形成的《04 执行前前置条件审查报告》，对 04 阶段文档进行修订与补全，产出一套“可再次复审、可判断是否允许启动 04”的修正版文档。**

注意：
这次不是让你重新做一遍泛泛审查，也不是让你直接进入 04 实施，而是要把“审查报告里指出的问题”真正落实成文档修订结果，让 04 从“当前不允许启动”推进到“具备再次复审基础”。

---

## 一、你的工作目标

你要完成的是下面这件事：

> **把当前 04 阶段文档，从“存在前置条件缺口、不能直接启动”的状态，修订为“前置条件定义清楚、任务拆解完整、验收口径明确、可再次进行启动审查”的状态。**

你必须以本次审查报告中的问题为直接依据，逐项修订 04 文档，而不是凭感觉重写。

---

## 二、修订依据

你必须以以下两类材料为依据进行修订：

### 1. 上位依据

* 总需求文档
* 01 / 02 / 03 / 04 / 05 阶段文档
* 与 04 相关的验收清单、审查报告、evidence 规范、hooks 验证方案、第一阶段基线材料等

### 2. 直接修订依据

* **本次已经形成的《04 执行前前置条件审查报告》**

你必须把这份审查报告中指出的所有关键问题，逐项转化为文档修订动作，尤其包括但不限于：

1. 04 启动条件与 blocked 处理流程语义冲突
2. 04 缺少 tasks / 执行计划 / 任务级拆解
3. 真实工程环境证据要求不清
4. required_checks schema 与总需求未对齐
5. phase2 启动批准真实性要求不清
6. CI 最小闭环与 evidence 绑定方式不清
7. 04 验收标准虽有方向，但缺少客观验证模板
8. 04 与 05 的边界虽大体合理，但 04 当前文档不足以支撑稳定衔接

---

## 三、你这次必须完成的成果

本次你不是只输出建议，你必须直接产出修订后的文档内容。
至少需要完成以下成果：

### 成果 A：修订 04 阶段需求文档

对当前 04 阶段文档进行修订，至少补齐：

1. **04 的启动许可条件**

   * 什么情况下允许启动
   * 什么情况下不允许启动
   * 什么情况下只能标记为 blocked，且不得进入实施

2. **04 的前置条件定义**

   * 哪些是进入 04 之前必须已经成立的
   * 哪些可以作为 04 内部首批任务处理
   * 哪些如果缺失会直接阻塞 04

3. **04 的范围、边界、输入、输出、完成标准**

   * 必须与总需求一致
   * 必须与 05 的边界清楚分离
   * 不能把本应在 04 落地的东西偷后移给 05

4. **04 的验收判定口径**

   * 必须写成可以客观判断的验收标准
   * 不能只写抽象目标
   * 必须明确证据要求

---

### 成果 B：新增或补齐 04 tasks 文档

你必须新增一份完整的 **04 tasks 文档**，不能再停留在阶段目标层。

tasks 文档必须满足：

1. 每个任务都要有明确编号

2. 每个任务都要写清：

   * 任务目标
   * 修改对象
   * 修改范围
   * 输入
   * 输出
   * 依赖关系
   * 验证方式
   * 完成标准
   * 失败/阻塞判定

3. 任务顺序必须合理，至少要体现清晰依赖链
   建议显式拆出类似：

   * 04-T1：启动条件与前置口径修订
   * 04-T2：总需求条款映射补齐
   * 04-T3：required_checks schema 对齐
   * 04-T4：phase2 启动真实性约束补齐
   * 04-T5：真实工程环境最小可用证据定义
   * 04-T6：Git 分支治理落地要求
   * 04-T7：CI 最小闭环要求与 evidence 绑定
   * 04-T8：真实示例归属与 demo_only 禁用约束
   * 04-T9：04 验收模板与证据清单
   * 04-T10：04→05 交接判定条件

你可以调整任务命名，但必须保持这个层级和完整度。

---

### 成果 C：补一份 04 验收证据模板

你必须补一份配套文档，名称可自定，但本质上要解决下面问题：

> **04 完成后，靠什么证据判断它是真的完成，而不是文档上完成？**

这份模板至少要写清：

1. 每类验收项需要什么证据
2. 证据保存到哪里
3. 命名规则是什么
4. 哪些是必须证据，哪些是补充证据
5. 哪些缺失时不得判定为完成

至少覆盖：

* schema 对齐证据
* Git 分支治理执行证据
* CI 运行证据
* evidence 链接关系
* 真实 feature / 非 demo 示例证据
* done 状态绑定证据

---

### 成果 D：补一份“04 启动前复审清单”

这份清单的目的不是执行任务，而是下一轮判断：

> **04 是否终于可以启动了？**

这份清单必须是复审者可直接使用的判断表，而不是说明文。
至少包含：

| 检查项 | 判定标准 | 证据来源 | 通过/不通过条件 | 是否阻塞启动 |

必须覆盖：

* 启动条件定义是否清楚
* 04 tasks 是否完整
* 总需求映射是否存在
* required_checks schema 是否对齐
* phase2 批准是否真实
* 是否已有真实工程环境最小证据
* CI/evidence 绑定是否可验证
* demo_only 示例是否被排除为 phase2 依据
* 04/05 边界是否仍清晰

---

## 四、修订要求：必须解决的关键问题

你必须逐项解决以下问题，不能遗漏：

### 1. 启动口径冲突必须消除

当前问题是：
04 文档里既写了“必须满足前置条件才能启动”，又写了“若不具备则 blocked”，导致执行者无法判断 blocked 是“可以进入 04 的一种状态”，还是“根本不允许启动”。

你必须把这两个概念彻底拆开：

* **启动许可条件**
* **启动失败时的处理动作**

不能再混写。

---

### 2. 04 必须从“阶段要求”变成“任务可执行”

当前最大问题之一是只有阶段文档，没有 tasks。
你必须把 04 转成真正可执行的任务级结构，而不是保持在宣告层。

---

### 3. required_checks 必须按总需求补齐结构

不能只说“要结构化”，必须明确结构字段。
至少要明确审查报告指出缺失的关键字段，类似：

* name
* shell
* cwd
* timeout_sec
* allow_network
* evidence_file
* stop_on_fail

你可以根据总需求原文再补，但不能比总需求更弱。

---

### 4. phase2 启动依据必须去“虚化”

你必须明确：

* 什么叫真实批准
* 什么不算真实批准
* phase1 / demo_only / placeholder 示例为什么不能作为 04/phase2 启动依据
* 需要哪些字段或记录，才能判定 phase2 启动真实性成立

---

### 5. 真实工程环境要求必须写实

不能只写“需要真实工程环境”，必须具体定义最小成立标准。
例如要明确：

* 至少要有怎样的工程载体
* 至少要有怎样的命令入口
* 至少要有怎样的运行/构建/测试证据
* 缺失哪些时不能进入 04 实施

注意：
你现在是在修文档，不是去伪造工程证据。
如果当前仓库不存在这些证据，你必须在文档里把它定义为“启动前必须补齐项”，而不是默认已满足。

---

### 6. CI 与 evidence 必须建立绑定规则

不能只说“要有 CI”，必须明确：

* 最小 CI 检查项是什么
* CI 通过与 done 的关系是什么
* evidence 如何引用 CI 结果
* 哪些检查失败时不得标 done

---

### 7. 04 与 05 的边界要再明确一次

虽然审查报告认为边界大体合理，但为了避免后续漂移，你必须在修订文档中明确写出：

* 哪些内容属于 04
* 哪些内容明确不属于 04
* 哪些内容留到 05 合理
* 04 完成后应该给 05 留下什么稳定前提

---

## 五、输出形式要求

你这次输出不能只是“建议修改如下”。
你必须直接给出：

### 第一部分：修订说明

说明你根据本次审查报告，修了哪些文档、为什么这样修、分别解决了哪些问题。

### 第二部分：修订后的 04 阶段需求文档全文

直接给出修订后的完整文档内容。

### 第三部分：新增的 04 tasks 文档全文

直接给出完整 tasks 内容。

### 第四部分：新增的 04 验收证据模板全文

直接给出完整内容。

### 第五部分：新增的 04 启动前复审清单全文

直接给出完整内容。

### 第六部分：修订映射表

请用表格明确写出：

| 审查报告指出的问题 | 本次修订落在哪份文档 | 如何修复 | 是否已解决 | 若未完全解决还差什么 |

---

## 六、重要限制

你必须遵守以下限制：

1. **不要推进 04 实施**
2. **不要写代码**
3. **不要伪造证据**
4. **不要把“缺失证据”写成“已完成”**
5. **不要只做表面润色**
6. **不能回避审查报告里指出的阻塞项**
7. **修订后的文档必须服务于“再次复审是否允许启动 04”**
8. **如果某项当前无法真实补齐，只能在文档中定义为“启动前必须补齐项”，不能偷改成已满足**

---

## 七、修订完成标准

只有同时满足以下条件，才算你这次任务完成：

1. 04 阶段需求文档已经消除启动口径冲突
2. 04 tasks 已经完整可执行
3. 04 验收证据模板已经建立
4. 04 启动前复审清单已经建立
5. required_checks / phase2 / CI / evidence / 真实工程环境 等关键缺口都已在文档中被明确化
6. 04 与 05 的边界在修订版中清晰可审
7. 下一轮复审者可以直接根据你的修订稿判断：04 是否允许启动

---

## 八、最终目标

你的输出最终必须服务于这件事：

> **让 04 从“当前不允许启动”推进到“文档层面已补齐，具备再次严格复审条件”。**

现在开始执行。
先基于审查报告逐项梳理修订动作，再输出修订后的完整文档全文，不要省略。
