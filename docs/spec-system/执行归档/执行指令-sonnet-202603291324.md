已确认以下事项，请你继续执行“首次 CI 触发与证据补齐”工作。

# 一、人工确认结果

1. **CI 平台确认**：采用 **GitHub Actions**
2. **触发方式确认**：当前允许使用 **`workflow_dispatch` 手动触发一次**
3. **允许范围**：

   * 允许你基于现有 `.github/workflows/step2-baseline-check.yml` 进行首次运行
   * 允许你在运行后补齐 CI 证据与映射
   * 不允许你扩展到 04 正式实施范围
   * 不允许你修改 05 范围
4. **phase2 批准仍未确认**

   * 你不得伪造 `phase2` 已批准状态
   * 相关字段仍保持“待人工确认”
   * 本次只推进 **E4（CI 运行证据）** 和 **E5 的 CI 映射补齐部分**

# 二、你现在要执行的任务

你现在只执行以下内容：

## 任务 A：触发首次 CI 运行

基于现有 workflow：

* `.github/workflows/step2-baseline-check.yml`

执行一次 **手动触发的 GitHub Actions 运行**。

如果你当前环境无法直接触发 GitHub Actions，也必须如实说明，不得伪造“已运行”。

---

## 任务 B：收集 CI 运行证据

运行后你必须收集并整理以下证据：

1. workflow 名称
2. 运行时间
3. run id / run 链接（若可得）
4. 每个 job 的执行结果：

   * `verify-feature-structure`
   * `verify-feature-json`
   * `verify-documents-exist`
5. artifact 是否生成：

   * `step2-engineering-baseline-structure-check`
   * `step2-engineering-baseline-feature-json`
   * `step2-engineering-baseline-documents`
6. summary 是否生成
7. 是否存在失败项；若失败，失败原因是什么

---

## 任务 C：补齐 evidence 与映射

如果 CI 运行成功或部分成功，你要继续做：

1. 更新 `features/step2-engineering-baseline/evidence/ci-status-*.md`
2. 更新 `features/step2-engineering-baseline/evidence/mapping-table-*.md`
3. 把映射补齐为真正的：

`required_checks.id -> CI 运行标识 -> evidence_file`

4. 更新 `features/step2-engineering-baseline/evidence/04-precondition-summary-*.md`
5. 明确把 E4 状态更新为：

   * 已关闭
     或
   * 部分完成
     或
   * 失败/阻塞

注意：

* **E2（phase2 批准）仍不得写成已关闭**
* 这次最多只能推进 **E4**，并补强 **E5 的 CI 部分**

---

# 三、严格限制

1. **不得伪造 CI 已运行**
2. **不得伪造 artifact**
3. **不得伪造 run id / run 链接**
4. **不得把 phase2 批准写成已确认**
5. **不得宣称“04 已完成”**
6. **不得推进 04 正式实施范围**
7. **不得修改 05 范围**
8. **若当前环境无法真正触发 GitHub Actions，必须明确说明卡点，不得假装成功**

---

# 四、你的输出要求

执行后请按以下结构汇报：

## 1. 本次执行结果

只能三选一：

* **CI 已成功触发并完成**
* **CI 已触发但部分失败**
* **当前环境无法真实触发 GitHub Actions**

## 2. 运行证据

列出：

* workflow 名称
* run id / run 链接（若可得）
* 运行时间
* 每个 job 结果
* artifact 结果
* summary 结果

## 3. 文件更新清单

列出你更新了哪些 evidence 文件，分别补了什么内容。

## 4. 当前状态判断

明确说明：

* E4 是否已关闭
* E5 的 CI 映射是否已补齐
* E2 是否仍待人工确认
* 当前是否达到“可再次发起 04 启动复审”的状态

## 5. 若未完成

明确写出卡点是什么，以及下一步需要人工做什么。

---

# 五、最终边界

你的角色是：

> **执行一次真实 CI 触发并补齐证据链的人**

不是：

* 替代人工批准 phase2 的人
* 宣称 04 已完成的人
* 扩展任务范围的人

现在开始执行。
