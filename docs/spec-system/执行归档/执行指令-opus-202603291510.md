已确认：现在进入 **phase2 批准写入与收尾更新**。
你本次不要推进 04 正式实施，不要扩展到 05，不要新增新的工程化范围。
你这次只做**批准信息落盘 + evidence 收尾更新 + 停止等待复审**。

# 一、人工确认结果（正式批准信息）

请将以下信息作为人工已确认的 phase2 批准结果写入：

* `phase`: `"phase2"`
* `phase_gate_approved`: `true`
* `phase_gate_approved_by`: `"user-confirmed"`
* `phase_gate_approved_at`: `"2026-03-29T14:30:00+08:00"`
* `phase_gate_note`: `"允许 step2-engineering-baseline 作为 04 启动前真实前提补齐后的真实接入对象进入 phase2，并据此发起新一轮 04 启动复审。当前批准仅覆盖启动前真实前提补齐闭环与复审准备，不代表 04 正式实施已获批准。"`

注意：

1. 这次批准**只覆盖**“04 启动前真实前提补齐完成后的复审准备”
2. **不代表** 04 正式实施获批
3. 不得把这次批准解释成“04 已完成”或“04 可直接实施”

---

# 二、你本次只允许执行的内容

你现在只执行以下 4 项：

## 任务 A：写入 phase2 批准字段

更新：

* `features/step2-engineering-baseline/feature.json`

要求：

1. 把上面的 phase2 批准字段按实写入
2. 保持 `demo_only` 仍为真实 feature 口径，不能回退
3. 不得顺手修改与本次批准无关的其他规则
4. 不得扩展 `required_checks` 范围
5. 不得新增 04 正式实施相关字段

---

## 任务 B：更新 evidence 汇总文档

更新：

* `features/step2-engineering-baseline/evidence/04-precondition-summary-*.md`

要求：

1. 将 E2 状态从“部分完成/待人工确认”更新为“已关闭”
2. 重新判断 E1~E5 状态
3. 重新判断 R1~R6 状态
4. 明确写出：

   * 当前已达到“可再次发起 04 启动复审”的状态
   * 但**仍不代表 04 已获实施批准**

---

## 任务 C：必要时更新映射或状态说明文档

如有必要，可同步更新：

* `features/step2-engineering-baseline/evidence/mapping-table-*.md`
* `features/step2-engineering-baseline/evidence/ci-status-*.md`

但注意：

1. 只允许做与“phase2 已获人工确认”直接相关的最小更新
2. 不得虚构新的 CI 结果
3. 不得补写不存在的 artifact
4. 不得因为收尾更新而改动已有真实执行记录

---

## 任务 D：输出最终收尾汇报

你更新完成后，必须停止，不要继续推进 04。
你只输出收尾汇报，供我决定是否发起新一轮 04 启动复审。

---

# 三、严格禁止事项

你严禁做以下事情：

1. **严禁宣称“04 已完成”**
2. **严禁宣称“04 现已允许实施”**
3. **严禁进入 04 正式实施任务**
4. **严禁扩展到 05**
5. **严禁新增新的 required_checks**
6. **严禁修改 CI 结果或伪造新运行**
7. **严禁修改与本次批准无关的 feature 范围、边界、规则**
8. **严禁把这次批准解释成全面批准**

---

# 四、你的输出要求

执行后请按以下结构汇报：

## 1. 文件更新清单

列出：

* 修改了哪些文件
* 每个文件改了什么
* 是否只做了最小必要改动

## 2. phase2 批准写入结果

明确列出写入后的字段值：

* `phase`
* `phase_gate_approved`
* `phase_gate_approved_by`
* `phase_gate_approved_at`
* `phase_gate_note`

## 3. 状态更新结果

明确说明：

* E2 是否已关闭
* E1~E5 是否现已全部关闭
* R1~R6 是否现已满足
* 当前是否达到“可再次发起 04 启动复审”的状态

## 4. 边界声明

必须明确写一句：

> 本次仅完成 phase2 批准写入与前置证据收尾更新，不代表 04 已完成，也不代表 04 已进入正式实施。

## 5. 最终结论

最后只允许给出以下两种之一：

* **已完成收尾更新，可发起新一轮 04 启动复审**
* **仍存在问题，暂不能发起 04 启动复审**

---

# 五、执行原则

你的角色是：

> **完成批准落盘与前置证据收尾更新的人**

不是：

* 宣布 04 已完成的人
* 推进 04 实施的人
* 扩展任务范围的人

现在开始执行，完成后停止，不要继续推进别的内容。
