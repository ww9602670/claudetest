现在不要执行阶段 04 或阶段 05 的落地工作。

先做一件更重要的事：

**依据总需求文档 + 当前 01/02/03 已完成状态 + 现有 04/05 阶段文档，系统性修缮并补全阶段 04、05 的任务书。**

目标：
让阶段 04、05 能完整承接总需求中“01~03 尚未完成、只部分完成、或尚未被正式验证”的内容，避免后续模型按不完整阶段文档继续漏项推进。

====================
一、你本次必须依据的文档
====================

只允许基于以下文档进行分析和修缮：

1. 总需求文档
- `docs/需求.md`

2. 已完成阶段文档与阶段审查结果
- `docs/spec-system/阶段需求/01-现状收敛与口径统一.md`
- `docs/spec-system/阶段需求/01-阶段审查报告.md`
- `docs/spec-system/阶段需求/02-第一阶段余量收尾与执行基线增强.md`
- `docs/spec-system/阶段需求/02-阶段审查报告.md`
- `docs/spec-system/阶段需求/03-第一阶段可审闭环增强.md`
- `docs/spec-system/阶段需求/03-阶段审查报告.md`

3. 当前待修缮的阶段文档
- `docs/spec-system/阶段需求/04-第二阶段工程化接入.md`
- `docs/spec-system/阶段需求/05-第二阶段自动化增强.md`

4. 如有需要，可读取以下与当前基线强相关的说明文档，但不要扩大范围：
- `docs/spec-system/验收清单.md`
- `docs/spec-system/验收清单审查报告.md`
- `docs/spec-system/hooks验证方案.md`
- `docs/spec-system/evidence目录规范.md`

除上述文件外，不要主动扩展阅读范围。

====================
二、你本次任务的核心判断依据
====================

你必须先基于上述文档，明确判断以下事实，然后再修缮 04、05：

1. 阶段 01、02、03 已经完成了什么
2. 阶段 01、02、03 哪些内容只做了部分落地
3. 阶段 01、02、03 哪些内容仍未开始
4. 这些“未完成 / 部分完成”内容中，哪些已经被当前 04、05 明确承接
5. 哪些总需求里的关键项，当前 04、05 还没有明确承接，或者只承接了一部分

注意：
- 不要只做泛泛总结
- 必须以“总需求条目是否被阶段任务书显式接住”为标准
- 你的目标不是解释，而是据此修改 04、05 文档

====================
三、你必须重点检查并决定归属的缺口项
====================

以下这些缺口，必须逐项判断它们应归入 04、05 的哪里；如果当前 04、05 没写，就必须补进去。

--------------------
A. 必须重点检查的结构化与状态机项
--------------------

1. `required_checks` 的完整结构化 schema 是否被后续阶段显式承接
至少包括：
- `id`
- `name`
- `shell`
- `command_windows`
- `command_bash`
- `cwd`
- `timeout_sec`
- `allow_network`
- `evidence_file`
- `stop_on_fail`

并且必须明确：
- `command_windows` 是权威字段
- hooks / 自动验证要基于结构化字段判断，而不是字符串模糊匹配

2. 状态机完整约束是否被后续阶段显式承接
至少包括：
- 固定状态机顺序
- 非法跳转拒绝
- 状态推进职责
- `done` / `blocked` 的正式行为闭环

--------------------
B. 必须重点检查的 hooks 与控制流项
--------------------

3. `UserPromptSubmit` 是否应在 04 或 05 被纳入
4. `Stop` / `SubagentStop` / `override_stop_guard` / `max_stop_blocks` 是否已充分承接
5. 自动修复循环的边界保护是否已完整承接
包括：
- `max_fix_rounds`
- 重复失败停止
- 新增失败数上升时停止
- 人工接管升级条件
- 缺少可执行验证命令时停止

--------------------
C. 必须重点检查的 command / skill / agent 项
--------------------

6. command 与 skill 冲突规则是否已被后续阶段显式承接
至少包括：
- 禁止同名 skill 与 command 并存
- command → skill 映射表
- command 只能作为兼容层

7. 背景知识类 / 目录专用 skill 的治理是否已被后续阶段显式承接
至少包括：
- `user-invocable: false`
- `paths`
- 重型工作流是否说明 `context: fork` 或等价隔离策略

8. agents / subagents 的完整职责体系是否已被后续阶段充分承接
至少包括：
- 主动使用时机
- 自动委派边界
- 工具范围
- 输出产物
- 最大工作边界

--------------------
D. 必须重点检查的工程化与验证项
--------------------

9. Windows shell 策略是否已在后续阶段被显式承接
至少包括：
- 默认优先 Node.js
- 禁止 bash-only 关键路径
- 必要 shell 时显式 `powershell`
- Windows 为第一优先验证平台

10. Git / CI / `done` 绑定是否已被 04 完整承接
11. 总需求中的“正式验证清单”是否已在 04 或 05 中被显式安排为最终总验收任务
12. 真实可运行示例是否有明确归属
如果 `example-login` 仍是占位示例：
- 必须明确后续在哪个阶段升级为真实示例
- 或在哪个阶段根据真实工程环境判断为 blocked
- 不能继续悬空

====================
四、你本次对 04、05 文档的修改目标
====================

你必须完成以下事情：

1. 修缮 `04-第二阶段工程化接入.md`
使其不仅覆盖 Git / CI / `done` 绑定，还要显式承接那些应属于“工程化接入阶段”的缺口项。

2. 修缮 `05-第二阶段自动化增强.md`
使其不仅覆盖 Stop / fix-loop / agents/subagents / 证据汇总，还要显式承接那些应属于“自动化增强阶段”的缺口项。

3. 如果某个缺口不应放在 04 或 05，而应该：
- 在 04 前作为前置条件先满足
- 或在 05 后另行立项
你必须在文档中明确写出来，而不是继续悬空。

4. 修完后，要让 04、05 两份文档满足这个标准：
- 读完它们，就能知道 01~03 剩余的关键缺口后续由谁承接
- 不再存在“总需求里明确写了，但后续阶段文档里没人负责”的事项

====================
五、允许修改范围
====================

只允许修改：

- `docs/spec-system/阶段需求/04-第二阶段工程化接入.md`
- `docs/spec-system/阶段需求/05-第二阶段自动化增强.md`

如确有必要，允许最小修改：
- `docs/spec-system/阶段需求索引.md`

但不要改动 01、02、03 的正文内容，不要回头重写已完成阶段。

====================
六、禁止事项
====================

严格禁止：

1. 直接开始执行阶段 04 或阶段 05 的实现工作
2. 修改任何 hooks / rules / skills / agents 代码或正文
3. 修改业务代码
4. 重写总需求文档
5. 重写 01 / 02 / 03 阶段文档
6. 生成新的长篇总报告替代文档修缮
7. 只在会话里分析，不落到阶段文档
8. 为了省事把漏项全部塞进 05
9. 擅自删除 04 或 05 的既有合理内容

====================
七、你修缮 04、05 时必须达到的标准
====================

修缮后的 04、05 必须满足：

1. 阶段边界清晰
- 04 负责工程化接入
- 05 负责自动化增强
- 不互相抢任务
- 不重复 01~03 已完成内容

2. 缺口承接明确
- 对于 01~03 只部分完成或未开始的总需求项，必须明确：
  - 归 04
  - 归 05
  - 或不归这两个阶段，需另立项 / 先 blocked

3. 与总需求对齐
- 修完后，04、05 应能清晰覆盖总需求中尚未落地的关键第二阶段内容
- 不允许继续存在“总需求有明确要求，但阶段文档没人接”的情况

4. 保持可执行
- 文档要像任务书，不是散文
- 必须保留：阶段目标、前置条件、输入、必须完成、允许修改范围、禁止事项、产出、完成判定、人工介入点、下一阶段
- 如需新增条目，必须加在这些结构内

====================
八、执行顺序
====================

你必须按以下顺序执行：

第一步：
先输出一个简短分析，只列：
- 04 当前已覆盖的缺口
- 05 当前已覆盖的缺口
- 当前 04、05 没接住的关键缺口清单

第二步：
直接修改 04、05 两份阶段文档，把漏项补进去、边界写清楚。

第三步：
输出修缮结果总结，只包括：
1. 04 补了哪些内容
2. 05 补了哪些内容
3. 哪些缺口被明确归属到 04
4. 哪些缺口被明确归属到 05
5. 哪些缺口明确不归 04/05，需要另行立项或 blocked
6. 修缮后是否建议立即推进 04，还是还要先补前置条件

====================
九、输出要求
====================

1. 全程中文
2. 先分析缺口，再改文档
3. 不要停留在会话口头建议
4. 最终一定要落到 04、05 文档中
5. 不要生成新总报告
6. 不要发散到实现层
7. 不要问我要不要继续，直接完成本次文档修缮