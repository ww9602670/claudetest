你现在的任务不是写代码，也不是继续泛泛润色文档。
你要基于**修订后的 04 文档集**，重新执行一次**严格的 04 启动复审**；并且：

* **如果复审结论为允许启动 04**，你必须继续往下直接产出一份**可交给 Sonnet 执行的正式任务包**
* **如果复审结论为有条件允许启动 04**，你必须明确哪些条件属于“启动前最后补齐项”，哪些任务可以先交 Sonnet，哪些绝对不能启动
* **如果复审结论为不允许启动 04**，你必须停止在复审结论，不要继续假装可执行，不要再做泛泛文档润色，而是明确列出仍阻塞启动的真实前提与证据缺口

---

# 一、你本次必须使用的审查材料

请基于以下材料开展工作，不能脱离这些材料空谈：

## 1. 上位与历史依据

* 总需求文档
* 01 / 02 / 03 / 04 / 05 阶段文档
* 01~03 的审查报告、验收清单、evidence 规范、hooks 验证方案、第一阶段 evidence 基线等

## 2. 关键审查依据

* 《04 执行前前置条件审查报告》

## 3. 本次修订后的 04 文档集

至少包括：

* `04-第二阶段工程化接入.md`
* `04-任务拆解与执行计划.md`
* `04-验收证据模板.md`
* `04-启动前复审清单.md`

你必须把这些文档当作本次复审的直接依据，而不是只复述上一次的结论。

---

# 二、你本次的核心目标

你要最终回答的是下面这个问题：

> **在“修订后的 04 文档集”基础上，04 现在是否已经具备启动实施的条件？**

并且你不能只停留在“文档写得更完整了”这个层面。
你必须进一步判断：

1. 文档层面的阻塞项是否已被修订关闭
2. 哪些问题只是“在文档里被定义清楚了”，但**现实前提仍未满足**
3. 当前是否已经具备：

   * 真实工程环境
   * 真实 feature / 非 demo_only 载体
   * phase2 启动真实性
   * CI 最小闭环可验证能力
   * evidence 链接能力
4. 如果这些现实前提未满足，那么即使文档修好了，是否仍应判定为**不能启动 04**

也就是说：

* **不能因为文档修好了，就自动判定可以实施**
* **必须再次检查“真实启动条件”是否成立**

---

# 三、你必须遵守的分流规则

## 分流 A：若复审结论为“允许启动 04”

你必须继续输出：

### A1. 启动通过结论

明确说明为什么现在允许启动，哪些关键前提已成立，依据是什么。

### A2. 可交给 Sonnet 的正式执行任务包

你必须直接产出一份给 Sonnet 的执行任务包，要求：

1. 任务包必须是**实施导向**，不是继续修文档导向
2. 要明确：

   * 执行目标
   * 执行边界
   * 可修改文件范围
   * 禁止修改范围
   * 任务顺序
   * 每任务完成标准
   * 每任务必需证据
   * 哪些任务失败时必须停止后续执行
3. 任务包必须足够清楚，使 Sonnet 不需要自己重新定义范围
4. 任务包必须和 04 文档集保持一致，不能擅自扩 scope
5. 任务包必须明确：**Sonnet 是执行者，不是重新定义规则的人**

### A3. Sonnet 执行前说明

你必须加一段简短说明，明确：

* 为什么现在可以交给 Sonnet
* Sonnet 可以做什么
* Sonnet 不可以做什么
* 遇到什么情况必须停止并回报

---

## 分流 B：若复审结论为“有条件允许启动 04”

你必须继续输出：

### B1. 有条件允许的结论

明确说明：

* 哪些条件已经成立
* 哪些条件还没成立
* 为什么不是“完全允许”，也不是“完全不允许”

### B2. 启动前最后补齐项清单

必须列成表格，至少包含：

| 优先级 | 补齐项 | 当前缺口 | 应由谁完成 | 完成标准 | 完成后如何确认 |

### B3. 可先交 Sonnet 的任务 / 不可交 Sonnet 的任务

必须分清：

* 哪些任务是可以先让 Sonnet 做的
* 哪些任务因为前提没成立，绝对不能让 Sonnet 做
* 哪些任务若先做，会造成伪闭环或返工

也就是说，你不能只是说“有条件允许”，而是要给出可执行分流。

---

## 分流 C：若复审结论为“不允许启动 04”

你必须输出：

### C1. 不允许启动的结论

明确说明当前为什么仍不能启动。

### C2. 仍阻塞启动的真实前提清单

这里必须重点区分两类问题：

1. **文档问题**
2. **事实问题 / 证据问题 / 环境问题**

你必须明确指出，当前究竟是：

* 文档仍有缺口
  还是
* 文档已补，但现实前提仍未成立

### C3. 禁止空转要求

如果你判断当前仍不允许启动，你必须明确写出：

* 现在不应该继续让 Sonnet 做 04 实施
* 现在也不应该继续泛泛润色文档
* 下一步应该补什么真实前提或真实证据

也就是：
**如果不允许启动，就给出阻塞项闭环路径，而不是继续空转。**

---

# 四、你这次必须重点审查的内容

## 1. 文档修订是否真的关闭了上轮审查问题

请逐项对照上一轮审查报告里的核心阻塞项，明确回答：

* 启动口径冲突是否已关闭
* tasks 缺失是否已关闭
* 验收证据模板缺失是否已关闭
* 启动前复审清单缺失是否已关闭
* required_checks schema 弱化问题是否已关闭
* phase2 启动真实性定义不清是否已关闭
* 04→05 交接条件不清是否已关闭

注意：
这里只能判定“文档问题是否关闭”，不能偷换成“现实问题已自动解决”。

---

## 2. 现实启动前提是否已经成立

你必须再次严格判断：

1. 是否已有**真实工程载体**
2. 是否已有**非 demo_only 的真实 feature**
3. phase2 批准真实性是否已有**真实证据**
4. 是否已有**最小工程入口**
5. 是否已有**CI 与 evidence 的真实可验证链**
6. 是否已有**可供 04 实施依赖的最小落地环境**

这里你必须非常严格。
**没有证据，就不能判定为成立。**

---

## 3. 当前是否已经适合交 Sonnet

你必须单独审查这个问题：

> **就当前状态而言，把 04 交给 Sonnet 去执行，是否安全？**

要明确回答：

* 会不会让 Sonnet 进入“边做边补前提”的伪执行
* 会不会让 Sonnet 实际上在重新定义规则
* 会不会让 Sonnet 因缺少真实环境而只能停留在文档动作
* 会不会导致 05 前置被提前污染

---

# 五、输出结构要求

你必须严格按以下结构输出，不要省略：

# 基于修订后 04 文档集的启动复审报告

## 一、最终结论

只能三选一：

* 允许启动 04
* 有条件允许启动 04
* 不允许启动 04

并给出 3~8 条核心理由。

---

## 二、复审依据

按类别列出：

* 总需求
* 01~03 阶段材料
* 上一轮 04 审查报告
* 修订后的 04 文档集
* 05 阶段文档

---

## 三、文档层阻塞项关闭情况复核

请用表格：

| 上轮阻塞项 | 本轮是否已关闭 | 依据文档 | 说明 |
| ----- | ------- | ---- | -- |

---

## 四、现实启动前提复核

请用表格：

| 真实启动前提 | 当前是否成立 | 证据 | 是否阻塞启动 | 说明 |
| ------ | ------ | -- | ------ | -- |

---

## 五、是否适合交给 Sonnet 执行

请单独写这一节，明确回答：

* 现在交给 Sonnet 是否安全
* 风险在哪
* Sonnet 会不会被迫补前提
* 是否会导致伪闭环

---

## 六、最终分流输出

这一节必须根据结论分三种情况输出之一：

### 情况 A：允许启动 04

输出：

1. 允许启动依据
2. 给 Sonnet 的正式执行任务包
3. Sonnet 执行限制与停止条件

### 情况 B：有条件允许启动 04

输出：

1. 条件成立项 / 未成立项
2. 启动前最后补齐项清单
3. 哪些任务可先交 Sonnet，哪些不可

### 情况 C：不允许启动 04

输出：

1. 仍阻塞启动的清单
2. 下一步应补的真实前提 / 真实证据
3. 明确说明：不要继续空转润色文档，也不要交 Sonnet 做 04 实施

---

## 七、最短行动建议

最后只给一个最明确的下一步建议，不要模糊表述。

---

# 六、重要限制

你必须遵守以下限制：

1. **不要为了推进而放宽标准**
2. **没有证据，不得判定真实前提成立**
3. **不要把“文档修订完成”偷换成“04 可以实施”**
4. **如果结论是不允许启动，不要继续产出 Sonnet 执行任务包**
5. **如果结论是有条件允许，必须清楚分流哪些能做、哪些不能做**
6. **如果结论是允许启动，任务包必须是实施型，不是继续修文档型**
7. **不要继续泛泛润色文档，除非它仍然是明确阻塞项**
8. **必须站在“避免 Sonnet 被迫伪执行、避免后续返工”的角度审查**

---

# 七、你的最终任务目标

你这次不是来重复写文档的，你这次是来给出一个**可决策、可分流、可直接进入下一步**的结论：

* 能启动，就直接给 Sonnet 任务包
* 不能启动，就明确卡点和补证据路径
* 有条件启动，就把能先做和不能做的边界切清楚

现在开始复审。
