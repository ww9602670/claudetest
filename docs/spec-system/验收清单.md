# 第一阶段验收清单（已签收）

审查日期：2026-03-28
修复日期：2026-03-28
收口日期：2026-03-28
审查范围：`H:\claude_worke` 当前工作区快照

标记说明：
- `[x]` 通过

对应目标：建立最小可运行的治理体系。

> **第一阶段口径已定，不再存在待拍板项。**

---

## 文件存在性检查

- [x] 1. `CLAUDE.md` 已重写，≤200 行，不含旧版嵌套命令字面
  审查：通过。文件 51 行，已将旧版命令字面改写为”禁止使用旧版嵌套命令体系”，字面清零。
  **已定口径**：阶段 01 允许修改范围内的执行基线文件不再出现旧版嵌套命令字面；仓库中其他历史迁移/审计/指令资料可保留旧字面，但不得作为当前执行入口、验收依据或治理事实源。
- [x] 2. `.claude/rules/governance-core.md` 已创建，含状态机约束
  审查：通过。状态机、批准协议、阶段闸门均已落地，文档已与代码同步。
- [x] 3. `.claude/rules/implementation-gate.md` 已创建，含 14 项前置检查
  审查：通过。已更新执行层级标注表，与 gate-write.js 实现一致。
- [x] 4. `.claude/rules/validation-loop.md` 已创建，含 max_fix_rounds
  审查：通过。`max_fix_rounds`、停止条件、证据链要求均已写入。
- [x] 5. `.claude/rules/docs-spec.md` 已创建，含双文档映射关系
  审查：通过。双文档映射与一致性检查要求完整。
- [x] 6. `.claude/hooks/gate-write.js` 已创建，Node.js 实现
  审查：通过。批准字段校验覆盖 approved/implementing/verifying 三个状态，含 approved_at ISO 格式校验与 approved_method 校验。
- [x] 7. `.claude/hooks/gate-bash.js` 已创建，Node.js 实现
  审查：通过。白名单模式采用双重防线（拒绝 shell 组合操作符 + 精确匹配完整命令），18/18 测试全过。
- [x] 8. hooks 运行时已配置
  审查：通过。
  **已定口径**：hooks 运行时加载位置为用户级 `~/.claude/settings.json`，项目级 `.claude/settings.json` 不是 hooks 注册位置，仅用于项目权限配置。项目文档对此件事的描述统一为”环境接入条件”，不得混写为项目级注册。
- [x] 9. `.claude/rules/spec-workflow.md` paths 已改为 `features/**`
  审查：通过。

---

## 模板完整性检查

- [x] 10. `docs/spec-system/模板/小白版/` 含 4 份模板（goal/plan/steps/acceptance）
  审查：通过。
- [x] 11. `docs/spec-system/模板/AI执行版/` 含 4 份模板（spec/design/tasks/verify）+ feature.json 模板
  审查：通过。feature.json 模板批准字段已统一为 approved_by / approved_at / approved_method / approved_note。

---

## Skills 检查

- [x] 12. `.claude/skills/spec/SKILL.md` 已创建
- [x] 13. `.claude/skills/design/SKILL.md` 已创建
- [x] 14. `.claude/skills/tasks/SKILL.md` 已创建
- [x] 15. `.claude/skills/implement/SKILL.md` 已创建（disable-model-invocation: true）
- [x] 16. `.claude/skills/verify/SKILL.md` 已创建（disable-model-invocation: true）

---

## 示例与文档检查

- [x] 17. `features/example-login/` 含完整 8 文件 + feature.json + evidence/
  审查：通过。
  **已定口径**：`example-login` 正式定位为**占位演示示例**，不作为真实业务闭环示例验收。第一阶段明确允许占位演示示例存在。真实可运行示例留到后续阶段，不在第一阶段要求。示例目录各文件已添加占位演示声明。
- [x] 18. `docs/spec-system/迁移说明.md` 已创建
  审查：通过。
- [x] 19. `specs/README.md` 已改为迁移说明
  审查：通过。

---

## 负向检查（禁止项确认）

- [x] A. 确认未修改任何业务代码
  审查：当前工作区无 `src/` 业务代码目录，第一阶段仅创建治理框架文件。
- [x] B. 确认未创建完整 agents 集合
  审查：通过。`.claude/agents/` 仅 3 个文件。
- [x] C. 确认未创建 deploy/release 能力
  审查：通过。
- [x] D. 确认未引入旧版嵌套命令字面、`feature.yaml`
  审查：通过。阶段 01 允许修改范围内的执行基线文件中旧版命令字面已清零，未发现 `feature.yaml`；仓库中其他历史资料残留不计入当前执行基线。
- [x] E. 确认未新增嵌套 `CLAUDE.md` / `.claude/rules/` / `.claude/skills/`
  审查：通过。
- [x] F. 确认未进入第二阶段
  审查：通过。

---

## 已定口径汇总（5 条）

| 口径项 | 已定结论 |
|--------|----------|
| 旧版嵌套命令字面 | 阶段 01 允许修改范围内的执行基线文件不再出现；历史迁移/审计/指令资料可保留，但不得作为当前执行入口或验收依据 |
| hooks 注册位置 | 运行时加载位置为用户级 `~/.claude/settings.json`；项目内文档只写”环境接入条件” |
| 批准字段命名 | 唯一合法命名：approved_by / approved_at / approved_method / approved_note，禁止 approval_* 变体 |
| example-login 定位 | 占位演示示例，第一阶段允许，不作为真实闭环验收 |
| 第一阶段占位示例 | 明确允许，真实示例留到后续阶段 |

---

## 审查结论

- 通过：25 项（含负向 6 项）
- 待人工拍板：0 项
- 不通过：0 项

**第一阶段最小闭环已签收。**

完整证据与历史审查记录见：`docs/spec-system/验收清单审查报告.md`
