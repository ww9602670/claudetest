你现在不要推进 04 正式实施范围，不要扩展到 05，不要擅自改规则。
你本次唯一任务是：

# 按《04 启动前真实前提补齐任务包》执行 P0-T2 ~ P1-T8

并且**严格保留人工确认点**，遇到需要人工拍板/人工批准/人工触发的节点时，必须停止并明确汇报，不能自行替代决策。

---

## 一、任务性质

这次任务**不是 04 实施任务**。
这次任务是：

> **为 04 再次启动复审补齐“真实前提 / 真实证据 / 真实环境”所需的执行性工作。**

你的工作目标不是宣称“04 已完成”，而是做到：

* 补齐真实 feature 的执行态载体
* 补齐 `feature.json` 的真实字段基础
* 确认最小工程入口
* 产出一次真实命令执行证据
* 建立最小 CI 基线
* 形成第一份 `required_checks.id -> CI -> evidence_file` 真实映射
* 汇总成可供下一轮 04 启动复审使用的证据包

执行完成后，目标状态是：

> **04 可以再次发起启动复审**
> 而不是
> **04 已经允许实施**

---

## 二、执行依据

你必须以以下材料为准执行：

1. 《04 启动前真实前提补齐任务包》
2. 修订后的 04 文档集：

   * `04-第二阶段工程化接入.md`
   * `04-任务拆解与执行计划.md`
   * `04-验收证据模板.md`
   * `04-启动前复审清单.md`
3. 现有仓库实际状态
4. 既有总需求与 01~03 阶段约束

若文档与仓库现实冲突，**必须以真实状态为准记录问题并上报**，不能假装满足。

---

## 三、你的执行范围

你本次只执行以下任务：

* **P0-T2：创建真实 feature 目录结构**
* **P0-T3：补齐真实 feature.json 并准备 phase2 真实批准入档**
* **P0-T4：确认最小工程入口**
* **P0-T5：跑通一次真实命令并留存证据**
* **P1-T6：建立最小 CI 检查链**
* **P1-T7：产出首份真实映射**
* **P1-T8：完成前置补齐证据汇总**

---

## 四、人工确认点

下面这些事项**不允许你自行拍板**，必须暂停并请求人工确认：

### 人工确认点 A：真实 feature 的选定结果

你**不能自己决定**哪个 feature 算“真实 feature”。
你必须使用我已指定/已确认的 `feature_id`。
如果当前上下文里没有明确给出真实 `feature_id`，你必须：

1. 停止进入 P0-T2
2. 明确说明：缺少 P0-T1 的人工拍板结果
3. 请求提供真实 `feature_id`

### 人工确认点 B：phase2 真实批准

你可以准备 `feature.json` 的字段结构与待填内容，
但你**不能自行伪造**这些批准字段：

* `phase: "phase2"`
* `phase_gate_approved: true`
* `phase_gate_approved_by`
* `phase_gate_approved_at`
* `phase_gate_note`

你可以把这些字段准备成“待人工填写/待确认”状态，
或者在我已明确提供批准信息时再写入。
没有人工批准信息时，不得擅自填成已批准。

### 人工确认点 C：CI 平台/触发方式

如果仓库当前没有明确的 CI 平台约束，你可以准备最小 CI 配置草案。
但如果 CI 是否启用、采用何种平台、是否允许触发运行需要人工确认，你必须暂停并说明。

---

## 五、执行目标与完成标准

### 目标 1：建立真实 feature 的执行态骨架

你需要在已给定 `feature_id` 前提下，创建或补齐：

* `features/<feature-id>/`
* 小白版 4 件套
* AI 执行版 4 件套
* `feature.json`
* `evidence/`

如果仓库已有同名目录，不能粗暴覆盖；要先审查现状，再按最小改动补齐。

### 目标 2：把 `feature.json` 补到“可进入人工批准前”的状态

你需要补齐基础字段，至少包括：

* `feature_id`
* `owner`
* `allowed_paths`
* `forbidden_paths`
* `required_checks` 结构化字段
* 其他与当前模板一致的必要基础字段

注意：

* `required_checks` 必须采用结构化 schema
* `command_windows` 为权威字段
* 不得保留弱结构/自由文本命令描述
* 未经人工确认，不得写成 phase2 已批准

### 目标 3：确认至少一条最小工程入口

你要在仓库中找出至少一条真实可执行命令，优先从以下类型中选：

* lint
* typecheck
* unit test
* smoke（若项目现实适配）

要求：

* 命令必须真实存在
* `cwd` 必须明确
* 不能猜测一个仓库里根本不存在的命令
* 如果没有可执行命令，必须如实汇报并停止后续依赖任务

### 目标 4：产出第一条真实执行证据

你要真实执行最小工程入口一次，并把输出写入 evidence。
要求：

* 输出必须是真实执行结果
* 可以成功，也可以失败，但必须真实
* 必须保留：

  * 命令内容
  * 执行时间
  * 退出码
  * 输出文件路径

不能伪造“成功记录”。

### 目标 5：建立最小 CI 检查链

你要根据仓库现实情况：

* 若仓库已有 CI 体系：补齐最小检查链
* 若仓库没有 CI：建立最小 CI 配置草案/配置文件

至少应覆盖 P0-T4 里确认的那条真实命令。
但如果 CI 平台选择或触发权限需要人工确认，必须停在该点并汇报。

### 目标 6：建立首份真实映射

你要产出第一份：

`required_checks.id -> CI 运行标识 -> evidence_file`

的真实映射表。
要求三者可以互相追溯，不能写成空表。

### 目标 7：形成前置补齐证据汇总

你要创建汇总文档，逐项回答：

* E1 是否关闭
* E2 是否关闭
* E3 是否关闭
* E4 是否关闭
* E5 是否关闭

并明确说明：

* 哪些已完成
* 哪些部分完成
* 哪些仍因人工确认点未关闭
* 当前是否已具备再次发起 04 启动复审的条件

---

## 六、每项任务的具体执行要求

### P0-T2：创建真实 feature 目录结构

要求：

1. 基于已确认 `feature_id` 创建目录
2. 若目录已存在，先审查已有文件，最小改动补齐
3. 建立完整基础结构
4. 创建 `evidence/` 目录
5. 输出本任务的变更清单

完成标准：

* 目录结构完整
* 不覆盖已有有效内容
* 有清晰的新增/补齐说明

---

### P0-T3：补齐真实 feature.json 并准备 phase2 批准入档

要求：

1. 基于仓库与模板补齐 `feature.json`
2. 明确 `demo_only` 不得为 `true`
3. 补齐 `required_checks` 最小结构化 schema
4. 对 phase2 批准字段：

   * 若已获得人工批准信息，则按实填写
   * 若未获得，则保留待填并显式标记“待人工确认”

完成标准：

* `feature.json` 基础字段完整
* `required_checks` 可用
* 没有伪造批准信息

---

### P0-T4：确认最小工程入口

要求：

1. 审查项目现有脚本/配置/工具链
2. 找出至少一条真实可执行命令
3. 写入 `required_checks`
4. 标注该命令为何被选为最小工程入口

完成标准：

* 至少一条真实命令
* 字段完整
* `cwd`、超时、是否允许联网等约束明确

若找不到真实命令：

* 必须停止 P0-T5、P1-T6、P1-T7、P1-T8
* 输出阻塞报告

---

### P0-T5：跑通一次真实命令并留存证据

要求：

1. 真实执行 P0-T4 选出的命令
2. 保存输出到 evidence
3. 文件命名清晰
4. 同时记录摘要信息

完成标准：

* 至少一份真实输出文件存在
* 输出与命令一致可追溯
* 不管成功失败，必须真实

---

### P1-T6：建立最小 CI 检查链

要求：

1. 根据仓库现状判断是否已有 CI
2. 若无，则创建最小 CI 配置
3. 配置至少覆盖 P0-T4 命令
4. 如需人工确认平台/触发，停下并汇报

完成标准：

* 存在 CI 配置文件或现有 CI 被接通
* 有运行记录或至少已准备到“待人工触发”状态，并明确卡点

---

### P1-T7：产出首份真实映射

要求：

1. 整理 `required_checks.id`
2. 整理 CI 运行标识
3. 整理 evidence 文件路径
4. 形成三元组映射表

完成标准：

* 映射表真实可追溯
* 不是模板空表
* 至少覆盖第一条真实检查链

---

### P1-T8：完成前置补齐证据汇总

要求：

1. 汇总所有证据路径
2. 对 E1~E5 逐项判断关闭情况
3. 对 R1~R6 条件逐项判断
4. 输出“是否可再次发起 04 启动复审”的明确结论

完成标准：

* 汇总文档完整
* 判断清晰
* 不夸大完成度

---

## 七、Sonnet 的禁止事项

你严禁做以下事情：

1. **严禁擅自选定真实 feature**
2. **严禁伪造 phase2 批准**
3. **严禁把待确认状态写成已批准**
4. **严禁伪造命令执行结果**
5. **严禁伪造 CI 运行结果**
6. **严禁因为想闭环而扩展到 04 正式实施范围**
7. **严禁修改 05 范围相关内容**
8. **严禁在缺少真实依据时自创规则**
9. **严禁把“前置补齐完成”写成“04 已完成”**

---

## 八、你的输出要求

你执行时必须产出：

### 1. 变更清单

列清楚：

* 新建了哪些文件
* 修改了哪些文件
* 每个文件改了什么

### 2. 执行结果分任务汇报

按任务分别汇报：

* P0-T2：完成 / 阻塞 / 未开始
* P0-T3：完成 / 部分完成 / 阻塞
* P0-T4：完成 / 阻塞
* P0-T5：完成 / 阻塞
* P1-T6：完成 / 部分完成 / 阻塞
* P1-T7：完成 / 阻塞
* P1-T8：完成 / 阻塞

### 3. 人工确认点清单

单独列出：

* 哪些地方需要我拍板
* 哪些字段需要我确认
* 哪些步骤需要我触发

### 4. 当前结论

最后只允许给出下面三种之一：

* **已完成前置补齐，可再次发起 04 启动复审**
* **部分完成，等待人工确认后可继续**
* **存在阻塞，当前不能继续**

---

## 九、最短执行路线

按这个顺序执行：

1. 检查是否已有明确 `feature_id`
2. 有则进入 P0-T2；没有则立刻停下请求人工提供
3. 完成 P0-T2 → P0-T3 → P0-T4
4. 若 P0-T4 找到真实命令，则执行 P0-T5
5. 再推进 P1-T6 → P1-T7 → P1-T8
6. 输出完整汇报，不要跳步

---

## 十、最终要求

你的角色是：

> **按既定任务包执行前置补齐工作的人**

不是：

* 重新定义规则的人
* 替代人工批准的人
* 偷偷推进 04 实施的人

开始执行前，先检查是否已明确给出真实 `feature_id`。
如果没有，请立即停止并只回复缺少该信息，等待人工确认。
