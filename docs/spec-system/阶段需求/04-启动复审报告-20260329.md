# 阶段 04 启动复审报告

**复审日期**：2026-03-29  
**复审指令**：执行指令-opus-202603290043.md  
**复审人**：Opus  
**复审轮次**：修订后 04 文档集的第一轮严格启动复审

---

## 一、最终结论

**结论：不允许启动 04**

### 核心理由（6 条）

1. **文档层阻塞项基本已关闭**，但这只说明"可审查口径更完整"，不等于"可实施"
2. 当前仓库下可见 feature 仅 `features/example-login`，其 `feature.json` 明确为 **`demo_only: true` 且 `phase: phase1`**，不满足 04 的真实启动前提
3. 未看到可证明"非 demo_only 真实 feature 已进入 phase2 且批准真实可追溯"的证据链
4. 未见可证明 04 依赖的"真实工程最小入口（build/test/check）+ 一次真实执行输出"的落地证据
5. 未见 CI 基线落地证据（例如 `.github/workflows` 不存在），无法建立 `required_checks -> CI -> evidence` 的真实可验证链
6. 现阶段若交 Sonnet 执行 04，会进入"边做边补前提"的伪执行，直接抬高返工与 05 边界污染风险

---

## 二、复审依据

### 总需求
- `docs/需求.md`

### 01~03 阶段材料
- `docs/spec-system/阶段需求/01-阶段审查报告.md`
- `docs/spec-system/阶段需求/02-阶段审查报告.md`
- `docs/spec-system/阶段需求/03-阶段审查报告.md`
- `docs/spec-system/验收清单.md`
- `docs/spec-system/evidence目录规范.md`
- `docs/spec-system/hooks验证方案.md`
- `docs/spec-system/第一阶段evidence基线.md`

### 上一轮 04 审查报告
- `docs/spec-system/阶段需求/04-执行前前置条件审查报告.md`

### 修订后的 04 文档集
- `docs/spec-system/阶段需求/04-第二阶段工程化接入.md`
- `docs/spec-system/阶段需求/04-任务拆解与执行计划.md`
- `docs/spec-system/阶段需求/04-验收证据模板.md`
- `docs/spec-system/阶段需求/04-启动前复审清单.md`

### 05 阶段文档
- `docs/spec-system/阶段需求/05-第二阶段自动化增强.md`

### 仓库现实状态补充证据
- `features/example-login/feature.json`
- `features/` 目录现状
- `.github/` 目录不存在

---

## 三、文档层阻塞项关闭情况复核

| 上轮阻塞项 | 本轮是否已关闭 | 依据文档 | 说明 |
| ----- | ------- | ---- | -- |
| 启动口径冲突 | ✅ 已关闭（文档层） | `04-第二阶段工程化接入.md` | 已分离"启动许可条件 / 禁止启动条件 / 预启动blocked / 执行中blocked" |
| tasks 缺失 | ✅ 已关闭（文档层） | `04-任务拆解与执行计划.md` | 10 个任务、依赖链、验收方式、阻塞判定已补齐 |
| 验收证据模板缺失 | ✅ 已关闭（文档层） | `04-验收证据模板.md` | 已有必需证据分类、模板、否决条件 |
| 启动前复审清单缺失 | ✅ 已关闭（文档层） | `04-启动前复审清单.md` | 已形成阻塞/非阻塞判定表 |
| required_checks schema 弱化 | ✅ 已关闭（文档层） | `04-第二阶段工程化接入.md` | 已声明 10 字段最小 schema（含 `name/shell/cwd/timeout_sec/evidence_file/stop_on_fail`） |
| phase2 启动真实性定义不清 | ✅ 已关闭（文档层） | `04-第二阶段工程化接入.md` | 已定义真实批准/无效批准及 demo_only 排除 |
| 04→05 交接条件不清 | ✅ 已关闭（文档层） | `04-第二阶段工程化接入.md`、`04-启动前复审清单.md` | 交接条件已显式列出，要求 05 不回补 04 基线 |

**文档层判定**：04 修订文档集已完成"可审查口径"闭环。本轮阻塞不再来自文档。

---

## 四、现实启动前提复核

| 真实启动前提 | 当前是否成立 | 证据 | 是否阻塞启动 | 说明 |
| ------ | ------ | -- | ------ | -- |
| 已有真实工程载体 | ❌ 不成立 | `features/` 仅见 `example-login` | ✅ 是 | 未见非 demo_only 的真实实施载体 |
| 已有非 demo_only 真实 feature | ❌ 不成立 | `features/example-login/feature.json` 中 `demo_only: true` | ✅ 是 | 明确为占位演示示例 |
| phase2 启动真实性有真实证据 | ❌ 不成立 | 同文件显示 `phase: phase1`，且 `phase_gate_note` 为"虚拟批准" | ✅ 是 | 不满足 04 要求的真实 phase2 批准 |
| 已有最小工程入口（可执行） | ❌ 未证实 / 视为不成立 | 仓库未见可对应真实 feature 的实施入口证据 | ✅ 是 | "没有证据即不成立" |
| CI 最小闭环可验证 | ❌ 不成立 | `.github/` 目录不存在 | ✅ 是 | 无 CI 基线可供 04 done 绑定 |
| CI 与 evidence 链接能力 | ❌ 不成立 | 未见真实 `required_checks -> CI -> evidence` 映射实例 | ✅ 是 | 仅有模板，无真实运行映射证据 |
| 可供 04 实施依赖的最小落地环境 | ❌ 不成立 | 上述前提连锁不成立 | ✅ 是 | 无法安全进入 04 实施态 |

**现实前提判定**：7 项真实启动前提均未成立，04 不具备实施条件。

---

## 五、是否适合交给 Sonnet 执行

**结论：现在交给 Sonnet 执行 04 不安全。**

### 风险分析

| 风险类型 | 具体表现 | 后果 |
|---------|---------|------|
| 角色偏离 | Sonnet 会被迫"先补真实前提、再做实施" | 从执行者变成规则补全者 |
| 伪执行风险 | 在缺少真实工程环境的情况下推进任务 | 形成文档完成但能力未落地的伪闭环 |
| 边界污染 | 04 工程基线未实证稳定即宣称完成 | 导致 05 被迫回补 04 内容 |

---

## 六、最终分流输出

### 情况 C：不允许启动 04

#### 1) 仍阻塞启动的清单

**A. 文档问题（当前状态）**
- ✅ 主阻塞项基本已关闭；文档层不再是主要阻塞。

**B. 事实问题 / 证据问题 / 环境问题（当前主阻塞）**

| 编号 | 阻塞项 | 当前状态 |
|-----|-------|---------|
| E1 | 无非 demo_only 的真实 feature 作为 04 工程化接入对象 | 当前仅 `example-login` 占位示例 |
| E2 | 无真实 phase2 启动批准证据 | 当前示例仍 phase1 + 虚拟批准 |
| E3 | 无真实工程入口及一次真实执行输出证据 | 未见 build/test/check 等命令记录 |
| E4 | 无 CI 基线与运行证据 | `.github/workflows` 不存在 |
| E5 | 无 `required_checks -> CI -> evidence` 的真实链路证据 | 仅有模板，无实例 |

#### 2) 下一步应补的真实前提 / 真实证据

| 优先级 | 补齐项 | 完成标准 | 完成后如何确认 |
|-------|-------|---------|--------------|
| **P0** | 指定 1 个真实 feature（非 demo_only） | 创建 `features/<feature-id>/` 及完整 8 文件 + `feature.json` | 仓库内可见真实 feature 目录结构 |
| **P0** | 完成 phase2 真实批准入档 | `phase: "phase2"`, `phase_gate_approved: true`, 批准字段完整可追溯, 非"虚拟批准" | `feature.json` 审查通过真实性检查 |
| **P0** | 跑通一次最小工程命令 | 有真实构建/测试/校验命令执行记录及输出 | 命令输出文件存在于 evidence |
| **P1** | 建立 CI 最小检查链 | lint/typecheck/unit 任一配置存在且有运行记录 | CI 配置文件 + 运行日志可见 |
| **P1** | 产出首份真实映射证据 | `required_checks.id -> CI 结果 -> evidence_file` 三元组可追溯 | 映射表存在于 evidence |

**补齐后的动作**：以上完成后，发起新一轮 04 启动复审（只需判"前提是否成立"，不是再润色文档）。

#### 3) 禁止空转要求

| 禁止项 | 原因 |
|-------|------|
| ❌ 不要继续让 Sonnet 做 04 实施 | 真实前提不成立，执行即伪执行 |
| ❌ 不要继续泛泛润色 04 文档 | 文档层已基本闭环，继续润边际收益低 |
| ❌ 不要把"文档修订完成"当成"04 可以实施" | 这是本轮复审核心判定逻辑 |

**正确动作**：先补齐真实前提与真实证据，再复审启动许可。

---

## 七、最短行动建议

**立即先落地一个非 demo_only 的真实 feature，并完成其 phase2 真实批准 + 一次最小工程命令执行证据；完成后再发起 04 启动复审。**

---

## 八、复审签署

- **复审人**：Opus
- **复审日期**：2026-03-29
- **复审依据指令**：执行指令-opus-202603290043.md
- **结论**：不允许启动 04
- **下一步**：补齐 P0 级真实前提后发起新一轮复审

---

## 附录：仓库现状快照（复审时点）

### Feature 目录现状
```
features/
└── example-login/          # demo_only: true, phase: phase1
    ├── goal.md
    ├── plan.md
    ├── steps.md
    ├── acceptance.md
    ├── spec.md
    ├── design.md
    ├── tasks.md
    ├── verify.md
    ├── feature.json        # 虚拟批准，非真实 phase2 依据
    └── evidence/
```

### CI 配置现状
- `.github/workflows/` 目录不存在

### 修订后 04 文档集（复审依据）
- ✅ `04-第二阶段工程化接入.md`（修订版）
- ✅ `04-任务拆解与执行计划.md`（修订版）
- ✅ `04-验收证据模板.md`（修订版）
- ✅ `04-启动前复审清单.md`（修订版）
