# 阶段需求 01 审查报告

审查日期：2026-03-28
审查对象：

- `docs/收口执行报告.md`
- `docs/spec-system/阶段需求/01-现状收敛与口径统一.md`
- 阶段 01 允许修改范围内的实际产出文件

## 审查目的

确认两件事：

1. `docs/收口执行报告.md` 的结论是否与项目实际产出一致
2. 阶段 01 是否已经形成可供后续阶段直接依赖的单一基线

## 审查发现

### 发现 1：原收口执行报告存在“结论过度外推”

原报告把旧版嵌套命令字面的结论写成了“仓内任何文件均不允许出现”。  
但实际仓库中，仍有部分历史迁移、审计、指令材料保留旧字面。

审查结论：

- 原说法不准确
- 按实际落地内容，正确口径应为：
  - **阶段 01 允许修改范围内的执行基线文件已清零**
  - 历史材料可保留，但不得作为当前执行入口或验收依据

### 发现 2：阶段 01 实际执行已吸收最小 hook 修复

原阶段 01 文档禁止“开始修 hook 逻辑漏洞”，但实际已经落地：

- `gate-bash.js` 白名单绕过修复
- `gate-write.js` 批准字段校验补齐
- 相关规则文档同步
- `tests/verify-hooks.ps1` 回归用例扩充到 18 项

审查结论：

- 执行过程确实越过了原始阶段 01 的边界
- 但这些变更已经真实落地并通过回归验证
- 为避免阶段文档与实际状态长期分叉，已将这些最小修复正式并入阶段 01

### 发现 3：阶段 01 的 5 个口径问题已实际落地

已核实：

1. hooks 注册归属已在 `docs/需求.md` 中写明
2. 批准字段命名已统一为 `approved_*`
3. `example-login` 已明确 `demo_only`
4. 第一阶段允许占位演示示例的口径已写入验收清单
5. 执行基线文件中的旧版嵌套命令字面已完成收口

## 回归验证

已执行：

- `tests/verify-hooks.ps1`

结果：

- 18 / 18 通过

其中包含：

- 白名单命令精确匹配
- `&&` 绕过拦截
- `;` 绕过拦截
- `|` 绕过拦截
- approved/implementing/verifying 状态下的写入校验

## 审查结论

按**校正后的阶段 01 口径**，阶段 01 现可判定为：

**已完成。**

原因：

1. 5 个口径问题已经落到仓库文件
2. 原本越界执行的最小 hook 修复已经被正式吸收进阶段 01
3. 回归测试通过
4. 后续阶段不再需要重新判断这些基线问题

## 对阶段 02 的影响

阶段 02 不应再重复：

- `gate-bash.js` 白名单绕过修复
- `gate-write.js` 批准字段校验补齐
- 相关规则与回归测试同步

这些项已完成，后续阶段应聚焦**剩余的第一阶段收尾与执行基线增强**。

## 下一步

下一活跃阶段：

- `docs/spec-system/阶段需求/02-第一阶段余量收尾与执行基线增强.md`
