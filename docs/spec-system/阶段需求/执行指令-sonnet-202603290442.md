按《04 启动前真实前提补齐任务包》继续执行。以下信息为本次执行所需的完整人工确认与执行边界，请以此为准，不要再等待 feature_id。

# 一、人工确认结果

已确认 P0-T1 的人工拍板结果如下：

1. 真实 feature_id：`step2-engineering-baseline`
2. 该 feature 为**真实 feature**，不是 demo_only
3. 该 feature 的用途是：作为《04 启动前真实前提补齐任务包》的真实接入对象，用于补齐 04 再次启动复审所需的真实前提与真实证据
4. 若仓库中不存在 `features/step2-engineering-baseline/`，允许你直接新建
5. 若仓库中已存在 `features/step2-engineering-baseline/`，禁止粗暴覆盖；必须先审查现状，再按最小改动补齐
6. 当前**尚未正式给出 phase2 批准结果**，因此你不得伪造已批准状态；只能先准备字段、结构、待确认内容
7. 当前你可以继续执行 P0-T2 ~ P1-T8 中所有**不需要人工拍板**的部分；遇到新的人工确认点时再暂停汇报

# 二、你现在要执行的任务范围

你本次只执行以下任务，不得扩展到 04 正式实施，也不得扩展到 05：

1. P0-T2：创建真实 feature 目录结构
2. P0-T3：补齐真实 feature.json，并准备 phase2 批准入档框架
3. P0-T4：确认最小工程入口
4. P0-T5：跑通一次真实命令并留存证据
5. P1-T6：建立最小 CI 检查链
6. P1-T7：产出首份真实映射
7. P1-T8：完成前置补齐证据汇总

# 三、phase2 批准的处理规则

当前 phase2 批准尚未由人工正式确认，因此你必须遵守以下规则：

1. 不得把 `phase` 直接写成已批准的 phase2 最终状态，除非我后续明确给出批准信息
2. 不得伪造以下字段：

   * `phase_gate_approved: true`
   * `phase_gate_approved_by`
   * `phase_gate_approved_at`
   * `phase_gate_note`
3. 你可以做的，是：

   * 预留这些字段
   * 按待确认状态组织结构
   * 在汇报中明确标注“待人工确认”
4. 如果你认为某个字段必须先有值才能继续，请使用“待人工确认”口径，不得自行造值

# 四、CI 的处理规则

1. 你可以审查仓库现状，并判断当前是否已有 CI
2. 如果没有 CI，你可以创建最小 CI 配置草案或实际配置文件
3. 但如果 CI 平台选择、触发方式、是否允许实际触发运行需要人工确认，你必须暂停并汇报
4. 不得伪造 CI 已运行成功的结果
5. 如果只能做到“已配置但待人工触发”，要明确写清当前停在什么位置

# 五、最小工程入口的处理规则

1. 你必须基于仓库真实现状寻找至少一条真实可执行命令
2. 优先从以下类型中选：

   * lint
   * typecheck
   * unit test
   * smoke（若现实适配）
3. 命令必须真实存在
4. 不得编造一个仓库里不存在的命令
5. 如果找不到真实可执行命令，必须立即停止以下任务并汇报：

   * P0-T5
   * P1-T6
   * P1-T7
   * P1-T8

# 六、禁止事项

你严禁做以下事情：

1. 严禁重新询问是否已有 feature_id，本条指令已明确给出：`step2-engineering-baseline`
2. 严禁擅自决定其他 feature 取代该 feature_id
3. 严禁伪造 phase2 批准
4. 严禁把“待人工确认”写成“已批准”
5. 严禁伪造命令执行结果
6. 严禁伪造 CI 运行结果
7. 严禁借前置补齐任务偷偷推进 04 正式实施范围
8. 严禁修改 05 范围
9. 严禁在缺少真实依据时自创规则
10. 严禁把“前置补齐完成”写成“04 已完成”

# 七、你的输出要求

执行结束后，你必须输出以下 4 部分内容：

## 1. 变更清单

列清楚：

* 新建了哪些文件
* 修改了哪些文件
* 每个文件具体改了什么

## 2. 分任务执行结果

逐项汇报：

* P0-T2：完成 / 阻塞 / 未开始
* P0-T3：完成 / 部分完成 / 阻塞
* P0-T4：完成 / 阻塞
* P0-T5：完成 / 阻塞
* P1-T6：完成 / 部分完成 / 阻塞
* P1-T7：完成 / 阻塞
* P1-T8：完成 / 阻塞

## 3. 人工确认点清单

单独列出：

* 哪些字段仍需我确认
* 哪些步骤需我手动触发
* 哪些卡点不是你能自行决定的

## 4. 当前结论

最后只允许给出以下三种之一：

* 已完成前置补齐，可再次发起 04 启动复审
* 部分完成，等待人工确认后可继续
* 存在阻塞，当前不能继续

# 八、执行顺序

按以下顺序执行，不要跳步：

1. 以 `step2-engineering-baseline` 作为唯一真实 feature_id
2. 执行 P0-T2
3. 执行 P0-T3
4. 执行 P0-T4
5. 若找到真实命令，则执行 P0-T5
6. 再推进 P1-T6
7. 再推进 P1-T7
8. 最后执行 P1-T8
9. 输出完整结果汇报

现在开始执行，不要再等待 feature_id。
