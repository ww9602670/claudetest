# 第一阶段验收审查报告（收口版）

审查日期：2026-03-28
修复日期：2026-03-28
收口日期：2026-03-28
审查对象：`docs/spec-system/验收清单.md`
审查范围：`H:\claude_worke` 当前工作区快照

## 总结

本次审查结果为：**第一阶段最小闭环已签收，5 个口径问题均已落定**。

## 5 个口径的已定结论

### 1. 旧版嵌套命令字面

**已定**：仓内任何文件均不允许出现旧版嵌套命令字面，包括禁用说明、迁移说明、历史对照，一律改写为不含旧字面的表述。

已执行：CLAUDE.md、docs/需求.md 中的旧命令字面已全部清零。

### 2. hooks 注册位置

**已定**：hooks 运行时加载位置为用户级 `~/.claude/settings.json`。

规则：
- 项目内文档只能把这件事描述为”环境接入条件”
- 项目级 `.claude/settings.json` 不是 hooks 注册位置，不得混写
- 已在 docs/需求.md 的”hooks 注册要求”章节修正

### 3. 批准字段命名

**已定**：唯一合法命名为：
- `approved_by`
- `approved_at`
- `approved_method`
- `approved_note`

禁止使用 `approval_by`、`approval_at`、`approval_method`、`approval_note` 等变体。

已执行：docs/需求.md 中的 `approval_note`、`approval_method` 已统一改为 `approved_note`、`approved_method`；feature.json 模板与 example-login/feature.json 均已使用正确命名。

### 4. example-login 定位

**已定**：`example-login` 正式定位为**占位演示示例**。

规则：
- 不作为真实业务闭环示例验收
- 示例文件已添加占位演示声明
- 第一阶段明确允许占位演示示例存在

### 5. 第一阶段是否允许占位示例

**已定**：允许。

验收清单第 17 项按”占位演示示例结构完整”验收，不要求真实代码闭环。真实可运行示例留到后续阶段自然产生。

---

## 本轮全部修复内容

### gate-bash.js 白名单绕过（已修复）

原问题：verifying 模式只提取命令首个词，可被 shell 拼接命令绕过。

修复：双重防线——先拒绝含 `&&`、`||`、`;`、`|`、`>`、`>>`、`<`、反引号、`$()` 的命令，再精确匹配完整命令。

验证：18/18 测试全过。

### gate-write.js 批准字段校验（已修复）

原问题：approved 状态未强校验批准字段；approved_at 未校验格式；approved_method 未校验。

修复：三个状态（approved/implementing/verifying）全部强校验；增加 ISO 格式校验；增加 approved_method 校验。

验证：5/5 测试全过。

### 规则文档与代码一致性（已修复）

- implementation-gate.md 执行层级标注已更新
- governance-core.md 批准字段检查范围已更新

### verify-hooks.ps1 测试用例（已补充）

新增 3 个命令拼接绕过用例（6a/6b/6c），覆盖 `&&`、`;`、`|` 三种绕过形式。

### 旧命令字面清零（已收口）

- CLAUDE.md 已清除旧版命令字面
- docs/需求.md 已清除旧版命令字面

### hooks 注册口径（已收口）

docs/需求.md hooks 注册要求章节已明确：用户级配置是运行时位置，项目级不是注册位置。

### 批准字段命名（已收口）

docs/需求.md 中 approval_note → approved_note、approval_method → approved_method 已统一。

### example-login 定位（已收口）

feature.json 已添加 demo_only 标记，各文档已添加占位演示声明。

---

## 逐项结论

### 正向 19 项

| 项 | 结论 | 说明 |
|----|------|------|
| 1 | 通过 | CLAUDE.md 旧命令字面已清零，改写为不含旧字面的禁止表述 |
| 2 | 通过 | 状态机完整，文档已更新 |
| 3 | 通过 | 文档已与代码对齐 |
| 4 | 通过 | max_fix_rounds 与停止条件存在 |
| 5 | 通过 | 双文档映射存在 |
| 6 | 通过 | 批准字段校验已补齐 |
| 7 | 通过 | 白名单绕过已修复 |
| 8 | 通过 | hooks 运行时位置为用户级配置，口径已统一 |
| 9 | 通过 | paths 已切到 features/** |
| 10 | 通过 | 小白版 4 模板齐全 |
| 11 | 通过 | AI 执行版 4 模板 + feature.json 模板齐全，字段命名已统一 |
| 12 | 通过 | spec skill 存在 |
| 13 | 通过 | design skill 存在 |
| 14 | 通过 | tasks skill 存在 |
| 15 | 通过 | implement skill 已禁模型自动调用 |
| 16 | 通过 | verify skill 已禁模型自动调用 |
| 17 | 通过 | 占位演示示例，定位已明确，第一阶段允许 |
| 18 | 通过 | 迁移说明完整 |
| 19 | 通过 | specs/README.md 已转为迁移说明 |

### 负向 6 项

| 项 | 结论 | 说明 |
|----|------|------|
| A | 通过 | 第一阶段仅创建治理框架，无业务代码修改 |
| B | 通过 | .claude/agents/ 仅 3 个文件 |
| C | 通过 | 未发现 deploy/release 落地能力 |
| D | 通过 | 允许修改范围内旧版命令字面已清零，未发现 feature.yaml |
| E | 通过 | 未发现嵌套结构 |
| F | 通过 | 仍处于 phase1 |

---

## 最终结论

**第一阶段最小闭环已签收。**

5 个口径问题均已落定，无待拍板项，无不通过项。后续阶段可直接基于本次结果继续推进，无需再次猜测口径。
