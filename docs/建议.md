建议.md（增强版，可直接交给 Claude 执行）
一、任务目标

请你基于当前项目中的 需求.md，对需求文档进行最小必要修改与结构修复，输出一份更新后的完整需求文档。

要求：

不得丢失原需求文档已有内容
不得重写整个文档框架
只能在必要位置：
修正结构问题
补充缺失硬约束
消除实现分叉
提升 Claude 后续执行稳定性
你的目标不是“另写一份新需求”，而是在原需求基础上做收口强化
二、修改原则
1. 最小必要修改

只改必须改的地方，不做风格化重写，不做无关优化，不扩展额外体系。

2. 保留原始内容

原需求文档中已成立、已确认、无冲突的内容必须原样保留。

3. 去分叉

任何会导致 Claude 后续实现时出现多种解释、多种落地路径的“可选方案”，都要尽量收口成唯一方案。

4. 生效优先

优先补那些会直接影响 Claude 执行稳定性的内容，而不是先补说明性文字。

三、必须先修复的内容
1. 修排版结构

这是最高优先级，必须先做。

请优先检查并修复以下问题：

第十五节中的代码块必须正确闭合
“十六”到“二十三”必须全部恢复成正式的 Markdown 二级标题，即 ##
“说明：”及后续约束不能混进代码块
补充标注段不能打断正文标题层级
整篇文档的标题层级必须清晰稳定，便于 Claude 扫描与遵守

说明：

这一步不是美化，而是硬性治理要求。
如果结构错乱，Claude 后续即使内容正确，也可能读取不稳定、遵守不稳定。

2. 把以下 3 条从“技术难点”正式提升为“需求硬约束”

请不要只在技术难点文档中保留这几条，而要把它们明确写入 需求.md 正文。

2.1 Hook 工具覆盖范围

必须明确写入：

PreToolUse
PostToolUse

对写入类工具的覆盖范围必须至少包含：

Edit
MultiEdit
Write

不能只写 Edit / Write，否则 MultiEdit 会绕过门禁。

2.2 command_windows 权威字段

必须明确写入：

command_windows 是权威执行字段
command_bash 只是可选兼容字段
两者语义必须等价
Windows 平台优先按 command_windows 执行与校验

不能让 Claude 把 command_bash 当主执行定义。

2.3 隐藏规则源审计

必须把隐藏规则源正式纳入审计范围，而不只是补充提示。

至少应纳入：

CLAUDE.local.md
.claude/settings.local.json
任何 @import 或等价外部引入
用户级 ~/.claude/...
其他潜在嵌套规则源

要求：

这些内容必须写进正式审计清单，不能只停留在附录说明中。

3. 增加“分阶段确认闸门”

这条建议应写入，但定位必须准确。

请在需求文档中新增：

阶段切换确认闸门

要求如下：

只拦截“第一阶段 → 第二阶段”的切换
不拦截阶段内部的自动推进
不替代批准协议
不替代状态机
不替代已有的阶段交付清单
它的作用只是“范围升级控制”

建议写法方向：

第一阶段最小落地完成后，Claude 默认不得自动进入第二阶段增强落地
只有在存在显式人工确认时，才允许进入第二阶段
这个确认必须写入 feature.json
建议字段可包括：
phase
phase_gate_approved
phase_gate_approved_by
phase_gate_approved_at
phase_gate_note

请注意：

这不是要求每一步都人工确认，而是只对阶段切换加一道确认门。

四、还需要补入需求文档的新内容

以下内容目前在原需求文档中缺失或不足，请一并补入。

4. Git 治理要求

请新增一节“Git 治理要求”，至少覆盖以下内容：

4.1 Git 是版本事实源

必须明确：

Git 是代码变更的唯一版本事实源
任何进入实现态的改动，都必须能映射到明确分支、明确提交、明确证据
4.2 分支策略

必须明确：

禁止直接在主分支上实现 feature
每个 feature-id 必须对应独立工作分支
分支命名规则必须与 feature-id 对应
4.3 提交策略

必须明确：

Claude 不得随意频繁提交
只有在达到某个阶段性完成条件后才允许提交
未通过必需 checks，不允许创建提交
提交信息必须包含：
feature-id
阶段信息
变更摘要
4.4 合并与回滚策略

必须明确：

未通过人工确认不得合并
未通过 CI 不得进入 done
必须定义失败后的回滚方式
自动化改动必须可通过 Git 提交记录和证据链回溯
5. CI/CD 治理要求

请新增一节“CI/CD 治理要求”。

注意分层：

5.1 第一阶段只要求 CI

请明确：

第一阶段必须把以下检查纳入 CI：
lint
typecheck
unit test
必要的 smoke（按项目适配）
done 状态不得只基于本地成功
必须以声明的 CI 结果为准
CI 结果必须进入证据链或可追踪记录
5.2 第二阶段再写 CD

请明确：

第二阶段才允许设计 CD
deploy / release 属于高风险动作
deploy / release 默认必须手动触发
不得在第一阶段引入自动部署
5.3 Claude 与 CI/CD 的关系

请补充：

Claude Code 可以与 GitHub Actions / GitLab 等流程配合
但 CI/CD 规则仍以项目需求文档、状态文件和项目级规则为准
Claude 不得绕过 CI/CD 门禁自行宣布交付完成
五、需要你检查并决定是否一并回写的内容

以下内容如果已经在最新需求文档中存在，请保留并校验是否足够明确；如果还不够明确，请补强措辞，不要重复造新规则。

6. 保持并强化以下既有硬约束

请检查以下条目是否已经存在并足够明确，不足则补强：

唯一执行态目录根：features/<feature-id>/
只允许扁平命令：
/spec
/design
/tasks
/implement
/verify
禁止 /project:*
只允许 feature.json
完整状态机：
draft -> review -> approved -> implementing -> verifying -> done | blocked
高风险 skill 必须手动触发
禁止嵌套规则源
auto memory 不是治理单一事实源
Windows 优先，默认 Node.js，不允许 bash-only 主路径
自动修复轮次受控
自动验证命令必须来自结构化 required_checks

如果这些已经写了，就不要重写，只做必要收口。

六、对更新后需求文档的输出要求

请在更新 需求.md 时遵守以下要求：

1. 输出完整版本

不要只输出 diff 或片段，要输出更新后的完整需求文档。

2. 保留原文

原需求里没有问题的段落必须保留，不得无故删减。

3. 显式说明改动点

在输出最终版前，请先简短列出你本轮修改了哪些关键点，例如：

修复了哪些结构问题
新增了哪些硬约束
哪些旧规则被收口为唯一方案
新增了哪些 Git / CI/CD 约束
4. 不要发散

不要借这次更新去引入无关的新体系、新目录、新工具、新平台假设。

七、你这轮任务的最终目标

你这轮不是重新设计治理体系，而是把现有 需求.md 从：

已经很强的治理方案说明书

进一步收口成：

更少分叉、更适合 Claude 直接执行的终极执行合同

重点是：

修结构
补硬约束
收口唯一方案
补 Git
补 CI/CD
补阶段切换确认门
把技术难点里已确认的工程规则正式升格到需求正文
八、执行顺序建议

请按以下顺序处理：

先修 Markdown 结构
再把 3 条技术难点升格为硬约束
再补阶段切换确认闸门
再补 Git 治理要求
再补 CI/CD 治理要求
最后检查全文是否还存在“可选方案导致分叉”的地方，并收口
九、完成标准

只有同时满足以下条件，才算你这轮建议执行完成：

更新后的 需求.md 仍保留原始内容骨架
结构性问题全部修复
技术难点中的关键工程结论已正式进入正文
分阶段确认闸门已写入且定位准确
Git 治理要求已补入
CI/CD 治理要求已补入
文档中不再残留会明显导致 Claude 分叉实现的关键可选项
更新后的版本可以直接作为 Claude 下一轮修改需求文档的输入依据