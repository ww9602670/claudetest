# required_checks -> 命令执行 -> evidence 映射表

**创建时间**: 2026-03-29
**feature_id**: step2-engineering-baseline
**状态**: 真实映射（基于实际执行）

---

## 映射关系总表

| required_checks.id | 执行时间 | 命令 | 退出码 | evidence_file | 结论 |
|-------------------|----------|------|--------|---------------|------|
| verify-feature-structure | 2026-03-29T04:42:49+08:00 | `ls features/step2-engineering-baseline/` | 0 | structure-check-20260329-044249.txt | ✅ 通过 |
| verify-feature-json | 2026-03-29T04:42:51+08:00 | `cat features/step2-engineering-baseline/feature.json` | 0 | feature-json-check-20260329-044251.txt | ✅ 通过 |
| verify-documents-exist | 2026-03-29T04:42:52+08:00 | `ls -la features/step2-engineering-baseline/*.md` | 0 | documents-check-20260329-044252.txt | ✅ 通过 |

---

## CI 映射（待人工确认触发后补充）

| required_checks.id | CI Job | CI 运行标识 | evidence_file | 状态 |
|-------------------|--------|-------------|---------------|------|
| verify-feature-structure | verify-feature-structure | 待触发 | - | ⏸️ 待人工确认 |
| verify-feature-json | verify-feature-json | 待触发 | - | ⏸️ 待人工确认 |
| verify-documents-exist | verify-documents-exist | 待触发 | - | ⏸️ 待人工确认 |

**说明**：CI 配置已创建，但触发方式需人工确认。确认后运行，将补充完整的三元组映射。

---

## 可追溯性验证

### 验证 1：required_checks.id → evidence_file
```bash
# 验证 verify-feature-structure 的证据文件存在
ls -la features/step2-engineering-baseline/evidence/structure-check-20260329-044249.txt
```

### 验证 2：命令 → 输出内容
```bash
# 查看 verify-feature-structure 的执行结果
cat features/step2-engineering-baseline/evidence/structure-check-20260329-044249.txt
```

### 验证 3：退出码 → 结论
- 退出码 0 → ✅ 通过
- 退出码 非0 → ❌ 失败

---

## 完整性检查

- ✅ 所有 required_checks.id 都有对应的命令执行记录
- ✅ 所有命令执行都有真实的 evidence 文件
- ✅ 所有退出码都被记录
- ✅ 三元组（id -> command -> evidence）可互相追溯
- ⏸️ CI 运行标识待人工确认触发后补充

---

## 映射表使用说明

1. **追溯某个检查项的结果**：
   - 从 required_checks.id 找到对应的 evidence_file
   - 查看该文件的内容

2. **验证 CI 运行结果**：
   - 从 CI 运行标识（待补充）追溯到 required_checks.id
   - 对比本地执行结果和 CI 执行结果

3. **验证 done 判定**：
   - 检查所有 required_checks 是否都为 ✅ 通过
   - 检查 CI 运行是否都为 ✅ 通过
   - 只有全部通过才允许标记 done
